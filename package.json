{
  "name": "human-gesture",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint && node scripts/lint/boundary-lint.mjs",
    "lint:boundary": "node scripts/lint/boundary-lint.mjs",
    "format": "biome format --write .",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@gesture/gesture-core": "workspace:*",
    "@gesture/protocol": "workspace:*",
    "turbo": "^2.10.12",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "happy-dom": "^20.14.0",
    "@playwright/test": "^1.63.0",
    "@biomejs/biome": "^2.5.12",
    "tsup": "^8.5.1"
  }
}
