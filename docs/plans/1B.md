# 1B — Perception pipeline — plan

Roadmap §4.2. Owner decisions 2026-09-07 (all option a, `docs/sdd/1B/driver.json`). Spec: [`1B.spec.md`](./1B.spec.md). Implementation: [`1B.impl.md`](./1B.impl.md).

Scope (owner Q1): the **browser-inference path only**. The ONNX-Web fallback is out of scope — a roadmap §9 re-plan trigger ("Intel Air < 20 fps on every path"); the final G8 GO/NO-GO is owner sign-off deferred until G3 lands; "delegate order" is a bench/fixture tunable, not plan material.

## The five questions (CLAUDE.md §1)

### 1. Placement
Two owning components, joined by one `protocol` message:
- **`apps/extension/entrypoints/offscreen`** — worker lifecycle, timed delegate selection + `webglcontextlost` recovery, adaptive 30/15 fps, restart-on-stream-end, and the per-frame composition that writes `GestureFrame` (trained label/score, extended 1€ landmark filtering, `palmFacing`).
- **`packages/gesture-core`** — the pure `MlpClassifier` (behind the existing `Classifier` seam; `KnnClassifier` retained as the conformance fallback), the pure `palmFacing` and `createLandmarkFilter` helpers, and — because the FSM is the single owner of gesture timing (CLAUDE.md §2) — the 3-frame vote and palm-facing gate in the XState machine.
- **Joining message:** `GestureFrame` in `packages/protocol` (extended additively, Q3). Offline training lives in `scripts/train/`.

Boundary tension resolved: arch §3.1 places "3-frame vote, palm-facing gate" in the offscreen classifier, but CLAUDE.md §2 / `.claude/rules/offscreen.md` forbid gesture-timing logic there and name the FSM as the single owner. **CLAUDE.md wins** — the per-frame palm-facing *boolean* is a geometric feature (offscreen, no timing) on `GestureFrame`; the *vote* and the *firing gate* live in the FSM (noted in `## Status`).

### 2. Boundary check
- `gesture-core` (MLP, palm-facing, landmark filter, FSM vote/gate) stays pure TS + `xstate` + `zod` + `@gesture/protocol` types; no DOM/`chrome.*`/`fetch`/timers. ✓ `.claude/rules/gesture-core.md`
- `offscreen` uses `@mediapipe/tasks-vision`, `gesture-core`, `protocol`, the runtime Port; no network/`chrome.storage`; video/`VideoFrame`/landmarks stay in the worker (weights are numeric, `palmFacing` a boolean). ✓ `.claude/rules/offscreen.md`, boundary-lint rule 1
- `protocol` change (`palmFacing`) depends on `zod` only. ✓ `.claude/rules/protocol.md`
- Training script is camera-free and under `scripts/`; **no new runtime dependency** (pure-TS MLP). ✓ CLAUDE.md §2, §4
- No rule file needs to change → none is edited in this PR (CLAUDE.md §5).

### 3. Interfaces touched
`GestureFrame` gains **one optional additive field**, `palmFacing?: boolean` (Zod schema in `packages/protocol` first, then offscreen writes it and the FSM/diagnostics read it). Additive — the frozen `gestureframe-v0` contract (non-strict `z.object`) still parses and 1A's "extend, none redefines" holds. `gesture-core`'s internal `FrameInput` gains an optional `palmFacing?: boolean`. No new `GestureLabel` members: the classifier's vocabulary **is** the frozen enum; the mock role-names are FSM/Intent mappings (Q3). No "Interfaces fixed here" in the roadmap row → no frozen interface, no contract (I) test.

### 4. Principle check (arch §1)
- **Two loops, two speeds** — inference stays worker-side; only the numeric `GestureFrame` crosses; adaptive fps protects the fast loop's CPU budget.
- **Video stays in one process** — landmarks/`VideoFrame` never leave the worker.
- **Page is hostile** — unchanged; the SW still Zod-validates every `GestureFrame` before the FSM.
- **Agent proposes, human disposes** — untouched.
- **Replaceable parts** — the `Classifier` interface is the seam; `MlpClassifier` swaps in, `KnnClassifier` stays as fallback; a later ONNX-Web path can replace worker inference behind the same `GestureFrame` output.

### 5. Tests
Fixture replay is the regression suite (`.claude/rules/gesture-core.md`). Golden replay suite fails loudly on threshold/weights change (E2); the training script prints the held-out precision/recall + false-fires/10 min table (E1, owner-deferred on mock); Playwright asserts 30/15 fps adaptation and `WEBGL_lose_context` recovery (E3). Unit tests cover MLP forward, palm-facing, landmark filter, FSM vote/gate (incl. backward-compat), fps-policy, and lifecycle restart.

## Exit checks

| # | Criterion (verbatim) | Kind | Check |
|---|---|---|---|
| E1 | Precision/recall table on held-out fixtures meets the G4 target | owner | - |
| E2 | replay suite in CI fails loudly on threshold change | mechanical | `pnpm vitest run packages/gesture-core/test/replay-golden.test.ts` |
| E3 | 30/15 fps adaptation verified in Playwright | mechanical | `pnpm exec playwright test -c apps/extension/playwright.config.ts adaptive-fps.e2e.ts` |

E1 is owner-verified/deferred (owner Q3): G4 is the MOCK row with no real fixtures or precision/recall targets, so "meets the G4 target (≥ 95 %)" cannot pass mechanically. The training script and its table are still built and run; E1 becomes a one-line owner confirmation once real recordings land. No `I` rows — the roadmap row has no "Interfaces fixed here" cell.

## Status

_Owned by the 1B session; rewritten, not appended._

**Done (session 0, plan):**
- Brainstorm (architectural) → three owner questions batched; answered 2026-09-07, all option (a) (`docs/sdd/1B/driver.json`).
- Spec (`1B.spec.md`), implementation plan (`1B.impl.md`), this plan, and the Exit checks table (E1–E3; no I-rows) written. Five-question checklist run on `1B.impl.md`.
- SDD workspace generated under `docs/sdd/1B/`.

**Decisions applied from the owner answers:**
- Scope = browser-inference path only; ONNX-Web = roadmap §9 trigger; final G8 = owner sign-off deferred to G3; delegate order = tunable (Q1).
- Classifier = offline-trained small MLP (weights JSON shipped, loaded in the worker) behind the `Classifier` seam, `KnnClassifier` fallback; in-browser retraining deferred to Phase 3 (Q2).
- Classifier vocabulary = the frozen `GestureLabel` enum; mock role-names are FSM/Intent mappings, no new protocol labels; proceed on MOCK G4 with E1 owner-deferred (Q3).

**Conflicts noted (CLAUDE.md §6):** arch §3.1 lists the 3-frame vote + palm-facing gate under the offscreen classifier; CLAUDE.md §2 requires all gesture timing/gating in the FSM. Resolved in favour of CLAUDE.md §2 — palm-facing *boolean* offscreen (per-frame geometry), *vote + gate* in the FSM. The gate is backward-compatible (`palmFacing===false` blocks, `undefined` passes) so the frozen `1C-fsm-state-tree` contract is preserved.

**Done (session 1, execute — SDD, 2-task cap):**
- Task 1 `[protocol]` `c94f481` — additive `GestureFrame.palmFacing?: boolean`; frozen `gestureframe-v0` contract untouched, 49/49 protocol tests. Review clean.
- Task 2 `[gesture-core]` `a506c4b` — pure `MlpClassifier` (behind the `Classifier` seam) + `MlpWeights`, `palmFacing()` geometry helper, `createLandmarkFilter()` (1€ on landmarks 0,4,8,9). 29 tests incl. frozen `1C-fsm-state-tree` contract; typecheck clean. Review clean.
- Two Minor findings and two ⚠️ rulings recorded in `docs/sdd/1B/progress.md` (palm-facing z-sign convention → carry to Task 5; landmark-filter `dCutoff` default → carry to Task 4). Neither is a Task-1/2 spec gap.

**Done (session 2, execute — SDD, 2-task cap):**
- Task 3 `[scripts]` — `scripts/train/train-gesture-mlp.ts` (seeded/deterministic SGD MLP; forward via gesture-core; held-out by `subjectId`; prints per-gesture P/R + false-fires/10min with N-frame voting), shipped `fixtures/models/gesture-mlp.json` (8-class, 63→16→8, `featureVersion=wrist-centered-63/v1`), `wxt.config.ts` copies it into `/models`, `fixtures/README.md` documents it + regeneration, `mlp-weights.test.ts`. Review clean (3 diagnostic-only nits on the owner-deferred E1 table, documented inline).
- Task 4 `[gesture-core]` — `FrameInput.palmFacing?`; 3-frame confidence vote + palm-facing gate in the FSM (arm+scroll; `palmFacing===false` blocks, `undefined`/`true` pass); `VOTE_FRAMES = 3`; `replayFixtureWith(record, classifier)` (default `replayFixture` unchanged). Golden replay suite `replay-golden.test.ts` (**E2**) with weights-sha256 + threshold locks; `vote-gate.test.ts`, `jitter-lag.test.ts`. Clutch timing preserved; frozen `1C-fsm-state-tree` + `gestureframe-v0` contracts untouched and green.
- Non-frozen consumer/unit tests updated for the vote (`machine.test.ts`, `replay-scroll.test.ts`, `apps/extension/test/fsm-wiring.test.ts`) — the vote applies to both arm and scroll (spec §5), so their 2-frame shortcuts were updated. Full repo: test 7/7, typecheck 7/7, lint + boundary OK.

**Done (session 3, execute — SDD, 2-task cap):**
- Task 5 `[offscreen]` `bc2494b` (+fix `7b3affb`) — worker robustness + wired the trained classifier. Cost-based delegate selection + `recreateHandLandmarker` (`mediapipe.ts`); worker fetches weights from the new `StartPump.weightsUrl` (extension origin) → `MlpClassifier` with `KnnClassifier` fallback, never throws the pump; pure `fps-policy.ts` gates inference so `PumpStat.fps` = inference rate (~30/~15) while every `VideoFrame` is read+closed; `webglcontextlost` → recreate; `gesture-frame.ts` injects the classifier, applies `createLandmarkFilter` pre-normalize, sets `palmFacing` on present-hand frames; `main.ts` resolves `models/gesture-mlp.json`; `background/fsm.ts` `toFrameInput()` forwards `palmFacing` (owner-authorized — closes the Task-4 carry so the gate is live in production). Review: spec ✅, quality approved; Important fix tightened `isMlpWeights` to require `featureVersion:string`.
- Task 6 `[offscreen]` `92dd8dd` (+fix `22f68c1`) — restart on stream end. Pure `lifecycle.ts` restart-storm guard (`shouldRestart`/`recordRestart`); worker posts `{type:'streamEnded'}` on reader done; `main.ts` dedupes worker `streamEnded` + track `ended` through a guarded `handleStreamEnd`. Review: spec ✅; Important fix replaced the permanent-kill-on-refusal path with a bounded `minIntervalMs`-spaced deferred retry (self-recovers once the storm window frees), plus three same-file minor cleanups.
- Rulings recorded in `docs/sdd/1B/progress.md` / journal: extension-origin weights `fetch` is not the "no network" boundary (spec §3); adaptive-fps + restart-guard constants live offscreen (CPU/lifecycle policy, not gesture timing) with lint-safe names; guard-refused restart self-recovers via deferred retry; `palmFacing` live z-sign confirmation is owner/real-camera work (helper uses the documented MediaPipe convention, gate is backward-compatible).

**Done (session 5, execute — SDD, Task 7 + final review):**
- Task 7 `[extension]` `5e3f778` (+review fix `9ecc4a5`) — `adaptive-fps.e2e.ts` (**E3**) + the `adaptive-fps` Playwright project. Verifies the fps-policy 30→15 downshift with the hand-less `bench/placeholder.y4m`: active windows `[18–20]` vs idle `[11–13]`. Asserts the machine-independent invariant (idle capped at the ~15 fps `idleFrameMs` ceiling, active materially above it) since headless detect cost caps the absolute active rate below 30. exit-check @ `9ecc4a5`: **E2 PASS, E3 PASS**, E1 OWNER (deferred); lock OK.
- Final whole-branch review (Opus 4.8, `master...HEAD`, high). One in-scope finding fixed (`9ecc4a5`); five out-of-scope findings surfaced (see `docs/sdd/1B/progress.md` + handoff): **2 confirmed correctness bugs** — `machine.ts` `Paused` re-arms in one frame (missing `entry: clearClutch`), and `inference.worker.ts` `webglcontextlost` recovery is dead code (listens on the worker global; Task 6's context-loss recovery is non-functional) — plus 3 minors (`mediapipe.ts` warm-up pins WASM; `isMlpWeights` ignores `featureVersion`; `palmFacing` mixes filtered/raw landmarks). Also: `frame-pump.e2e.ts` (0B G1 gate) now fails from Task 5's adaptive fps (hand-less fixture → 15 fps idle cap < its 28 fps floor); not a 1B exit check, not in the `vitest` run.

**Done (session 6, execute — SDD, owner-authorized fix tasks, 2-task cap):**
- Task 8 `[gesture-core]` `309ebee` (finding 1) — `entry: clearClutch` on the FSM `Paused` state, symmetric with `Armed`, so a pause-while-held cannot re-arm within 1 frame (it left a stale `clutchStartTs` re-satisfying `clutchElapsed`). TDD `pause-rearm.test.ts` (fixture replay) proves the fix; frozen `1C-fsm-state-tree` contract byte-unedited and green; golden replay (E2) green.
- Task 9 `[offscreen]` `1d2ee40` (findings 2 + 3 + 4) — **F2:** the worker now owns the `OffscreenCanvas` MediaPipe binds its GL context to (`VisionTaskOptions.canvas`) and listens for `webglcontextlost` on **that surface** (the old listener was on the worker global `self`, dead code), so `recreateHandLandmarker` actually runs; a `VITE_TEST_HOOKS` BroadcastChannel hook lets `adaptive-fps.e2e.ts` drive a real `WEBGL_lose_context` and assert recovery-survival. **F3:** new pure `delegate-cost.ts` — the delegate downgrade needs a *sustained* over-budget signal past a warm-up window, so a cold shader-compile spike no longer pins WASM for the worker's life. **F4:** new pure `classifier-select.ts` — `classifierFromWeights` compares `featureVersion` to the runtime layout and falls back to `KnnClassifier` on mismatch. New unit tests for both helpers; extension 47 tests green.
- Rulings (`docs/sdd/1B/progress.md` / journal): recovery e2e asserts the machine-independent survival invariant (concrete delegate re-init stays owner real-browser); `main.ts` deliberately untouched (out of Task-9 scope — the e2e drives the worker via the same-origin SW→worker BroadcastChannel); the E3 file now builds with `VITE_TEST_HOOKS=1` (inert for the frozen 30/15 assertion, verified PASS). Findings 1–4 addressed; finding 5 (`palmFacing` filtered/raw mix) + live `palmFacing` z-sign remain owner/real-camera deferrals.

**Done (session 7, execute — SDD, Task 10):**
- Task 10 `[extension]` `bace638` — made `frame-pump.e2e.ts` (the 0B G1 gate) idle-aware: it measures p05 over the **active seed window only** rather than the whole 60 s run. Task 5's adaptive fps-policy holds the active rate for `idleWindowMs` (5 s) from pump start, then — with the hand-less fixture — deliberately downshifts to ~15 fps; the old p05-over-60 s averaged in that intended idle tail (< 28) so the gate could not pass on any hardware. The rewrite recovers the pump-start seed from the first PumpStat window, keeps the fully-active windows before the downshift (dropping the cold warm-up window and the boundary straddle), asserts `p05 ≥ 28` there, and adds a relative active-vs-idle cross-check. Scope-clean (only the test file; no gesture-timing constants).
  - Ruling: the absolute 28 fps stays the **owner-hardware** G1 claim (`spike-results §G1` GO). Observed headless @ `bace638`: `active p05 = 21.5`, `idle ≈ 12` — the webgl delegate's per-frame detect cost caps even the active rate below 28 (session-5's "~20 fps in headless"). The fix is verified by the correct active/idle split and the relative cross-check passing headless (21.5 > 11.7); the remaining red on the absolute 28 is the owner-hardware re-confirmation signal, not a regression. Not a 1B exit check and not in the `vitest` run, so `pnpm test` and `exit-check 1B --fast` stay green.

**Done (session 8, finish):**
- Clean verification (CLAUDE.md §5): typecheck 7/7, `pnpm lint` 5/5 + boundary-lint OK, `pnpm test` green (gesture-core 50, extension 47, playground 22). `scripts/milestone/exit-check 1B` (full) = **2 PASS 0 FAIL 1 OWNER 0 other, lock OK** — E2 PASS, E3 PASS, E1 OWNER (deferred on MOCK G4). No FAIL/TAMPERED.
- `obra-finishing-a-development-branch`: pushed `1B`, opened **PR #8 → `master`** (https://github.com/duylongpro99/gesture2browse/pull/8). Never merged (owner disposes). Worktree preserved for PR feedback + the owner real-hardware follow-ups.

**In progress:** none. Impl tasks 1–7 and owner-authorized fix tasks 8–10 committed on branch `1B`; PR #8 open.

**Next:** owner reviews/merges PR #8. Then two owner real-hardware runs: the real-browser context-loss check (confirms finding 2's `webglcontextlost` recovery end-to-end) and the owner-hardware G1 re-confirmation (`pnpm exec playwright test -c apps/extension/playwright.config.ts frame-pump.e2e.ts` → `[G1]` active p05 ≥ 28). On merge, owner logs the §8 row below via `scripts/milestone/log-decision` and removes the STATUS row.

**Blockers:** none for planning. Milestone-level: real G4 fixtures (E1) and the G3 bench (final G8 sign-off) remain owner/external and are out of 1B's build scope by decision.

**Proposed decisions for roadmap §8 (owner logs; agent does not edit §8):**

> | 2026-09-07 | **1B perception = browser-inference path only.** Small offline-trained MLP classifier (weights JSON shipped, loaded in the offscreen worker) behind the `Classifier` seam (`KnnClassifier` fallback); classifier vocabulary = the frozen `GestureLabel` enum (no new labels); `GestureFrame` extended additively with `palmFacing?:boolean`; 3-frame vote + palm-facing gate in the FSM (single timing owner, CLAUDE.md §2), backward-compatible with the frozen contracts. ONNX-Web fallback out of scope (roadmap §9 trigger: Intel Air < 20 fps on every path); final G8 GO/NO-GO deferred to owner sign-off once G3 lands; delegate order is a bench tunable. In-browser retraining deferred to Phase 3 (track 3.1). NOTE: planned/executed against the **MOCK** G4 row to test-drive the flow — Exit E1 (precision/recall ≥ 95 %) is owner-deferred until real fixtures land. | 1A merged; G4 (MOCK); owner answers 2026-09-07 (spec §1–§5) | Fixed in `docs/plans/1B.md`; extends `GestureFrame`; consumed by 1C/1D/1E/2A |
