# 1C — Page plane and actions — Plan

Page-side execution of the pointer plane: cursor overlay, interactable index + snapping, the full `Armed.*` gesture FSM, and a dispatcher that executes trusted (CDP) or synthetic input plus browser navigation. Roadmap row: `docs/05-roadmap.md §4.3`. Design: `1C.spec.md`. Tasks + code: `1C.impl.md`. Deviation: `docs/adr/0001-pointer-plane-via-sw-relay.md` (proposed).

Plan inputs (all in roadmap §8): 1A merged; **G5** dispatch default = CDP (2026-09-05); **G6 (MOCK)** snapping ON / snap radius 40 px / pinch default / dwell 600 ms Accessibility (2026-09-12). Owner answers (2026-09-12): Q1 CDP-when-granted + synthetic fallback; Q2 pointer via `PageCommand.pointer` SW-relay (ADR 0001); Q3 back/forward via swipe, tab/zoom capability-only.

## 1. Placement

Three owning components joined by `packages/protocol` messages, plus one library. One owner each:
- **`packages/page-index`** (rule `page-index.md`): interactable index (grid hash, `MutationObserver`, visibility filter, iframe geometry) + snapping (speed-scaled radius, neighbour hysteresis, pointer latch); owns the numeric **interactable id scheme** shared with 2A's a11y snapshot.
- **`packages/gesture-core`** (rule `gesture-core.md`): the full `Armed.*` state tree with all hold timers, cooldowns, pinch hysteresis, the 300 ms stable-tracking gate, dwell timing, and scroll inertia. **Sole owner of gesture timing** (CLAUDE.md §2).
- **content script** (`entrypoints/content/**`, rule `content.md`): cursor overlay (closed shadow root, rAF), snapping wiring, `snapshot` answer, `highlight`/`preview` render, and the synthetic **fallback** dispatch path.
- **service worker** (`entrypoints/background.ts` + `background/**`, rule `background.md`): dispatcher (Intent→CDP trusted input or synthetic fallback), CDP attach/detach lifecycle, pointer relay, hover merge into the FSM, scroll-inertia forwarding, back/forward/tab/zoom executors, and hardening (port reconnect, SPA re-injection).

Joining messages (`protocol`): `PageCommand` (SW→CS: pointer/highlight/preview/snapshot/fallbackClick added), `PageEvent` (CS→SW: hover/snapshot added), `A11yItem` (new; the id scheme), `CursorState` (new enum). `Intent` is internal (gesture-core→SW dispatcher), extended with Click/DragStart/DragEnd/Swipe/HoldGesture. The pointer plane is `PageCommand.pointer` over the existing `ServiceWorkerToContent` port (ADR 0001); the reserved `OffscreenToContent` port stays reserved.

## 2. Boundary check

Every needed import/API is allowed by the owner's rule file (details per task in the impl plan):
- **protocol:** `zod` only; new shapes defined here first, names from arch §6 (`A11yItem`, `PageCommand`, `PageEvent`, `Intent`, `CursorState`). ✓
- **page-index:** DOM APIs + `@gesture/protocol` types (`A11yItem`); **no** `chrome.*`/network/React; jsdom/happy-dom testable; the id scheme + spatial tunables (`SNAP_RADIUS_PX` etc.) live here. ✓
- **gesture-core:** pure TS, `xstate` (incl. `after` for dwell/inertia — timers *inside* XState are allowed), `@gesture/protocol` types; **no** DOM/`chrome.*`/`fetch`; all new timing/hysteresis/cooldown constants in `constants.ts`, read only by the machine. ✓
- **content:** `@gesture/page-index`, `@gesture/protocol`, DOM, Shadow DOM; validates every `PageCommand` with Zod; overlay in a **closed** shadow root; synthetic `dispatchEvent` is the fallback path only; **no** `chrome.storage`/`chrome.debugger`/`fetch`/secret/gesture timing. ✓
- **background:** `@gesture/gesture-core`, `@gesture/protocol`, `chrome.*`, **`chrome.debugger`** (CDP is the primary input path per the rule; optional permission requested on first need); validates every inbound Port message with Zod; **no** DOM/React/MediaPipe/secrets. ✓

One deviation: routing the pointer through the service worker contradicts arch §3.2 ("pointer moves do not pass through the service worker"). It is not a `.claude/rules/` line or a CLAUDE.md §1–2 hard rule; it is an architecture-doc design deviation, recorded as **ADR 0001 (proposed)** — execute proceeds only after the owner accepts it at PR review.

## 3. Interfaces touched

Zod in `protocol` first, then both sides (impl Task 1, then 2/5/6). All additive; nothing 1A froze is redefined (roadmap §2.1):
- `CursorState` — **new** enum (`idle|pointing|snapped|pinch|drag|paused`), rides in `PageCommand.pointer`.
- `A11yItem` — **new** (`{id,role,name,value?,bbox,state?}`); freezes the numeric id scheme (stable per page load), consumed by 2A.
- `PageCommand` — **adds** `pointer` (ADR 0001), `highlight`, `preview`, `snapshot`, `fallbackClick`; `scroll` unchanged.
- `PageEvent` — **adds** `hover`, `snapshot`; `ready` unchanged.
- `Intent` — **adds** `Click{id}`, `DragStart{id}`, `DragEnd`, `Swipe{dir}`, `HoldGesture{kind}`; `Arm|Pause|Scroll` unchanged. Scroll inertia is realized as decaying `Scroll{dy}` intents (no new member).
- FSM state tree — **extends** `Armed` with `Pointing`, `PinchDown`, `Dragging`, `SwipeArmed`, `Hold` (arch §5); `Paused`/`Armed.{Idle,Scrolling}` unchanged. Consumed by 2A (attaches `Agent.*`).

No `Action`/`Proposal` protocol type here — those are 2A's to fix (roadmap §5.1); 1C's back/forward/tab/zoom are internal dispatcher executors.

## 4. Principle check (`02-architecture §1`)

- **Two loops, two speeds** — 1C is entirely the fast perception–control loop; no agent, no network.
- **Video stays in one process** — 1C touches no video; only `GestureFrame` (no landmarks) enters the SW, only `PageCommand`/`PageEvent` cross to the page.
- **The page is hostile** — content holds no secrets/authority, runs only SW-sent, Zod-validated `PageCommand`s; CDP (trusted input) and all policy live in the SW; the overlay is a closed shadow root.
- **Agent proposes / human disposes** — n/a to direct control; 1C fixes the `HoldGesture` intents, the a11y id scheme, and the highlight/preview/snapshot commands that 2A's observe/confirm path needs.
- **Replaceable parts** — index, snapping, FSM, dispatcher, and the CDP-vs-synthetic executor sit behind protocol messages / small interfaces; the pointer route is swappable (ADR 0001 keeps the direct port reserved).

## 5. Tests

- **E1** Fitts e2e (Playwright, fake camera): scripted `GestureFrame` pointer path acquires 40 px targets; asserts median acquisition ≤ 1.8 s and click precision ≥ 95 % (roadmap §4.6).
- **E2** XState model tests (gesture-core): the full `Armed.*` tree, cooldown/hysteresis invariants, and fixture replays for pinch-tap/dwell/drag/swipe/hold with no false fires.
- **E3** SW-kill recovery e2e (Playwright): kill the service worker, assert the pointer/click path recovers.
- Supporting: snapping property tests (page-index), overlay + synthetic-dispatch unit (content), dispatcher/CDP unit with `chrome.*` fakes (background), trusted-vs-synthetic click-count e2e.
- Five frozen contract tests (below), one per interface fixed here, written from 2A's side. A threshold/gesture change replays the fixture suite (`fixtures/gestures/`).

## Five-question checklist on `1C.impl.md`

1. **Placement** — every task names one owning component (T1 protocol; T2 gesture-core; T3–T4 page-index; T5 content; T6–T7 background; T8 extension test). Cross-component work goes through a `protocol` message. ✓
2. **Boundary check** — each task carries a Files/Interfaces block checkable against its rule file; the only disallowed-by-default API is `chrome.debugger` in background, which its rule explicitly allows; the one deviation (pointer via SW) is ADR 0001. ✓
3. **Interfaces** — Task 1 changes the `protocol` Zod schemas first; Tasks 2/5/6 change both sides after. ✓
4. **Principles** — §4 above; fast loop only, video untouched, page hostile, replaceable seams. ✓
5. **Tests** — E1 (T8), E2 (T2), E3 (T8), snapping/overlay/dispatcher units (T4/T5/T6), five contract tests (T1–T2). No TODO hides an architectural question; the reserved direct port is a documented ADR-0001 deferral, not a stub; scroll inertia and dwell timing are pinned to gesture-core (no second timing owner). ✓

## Exit checks

Frozen at plan time. `scripts/milestone/exit-check 1C` runs these after every session; a later edit to this table or to a contract test it names is reported as `TAMPERED` (re-plan, never a quiet fix). `E` criteria are verbatim from the roadmap §4.3 Exit cell (split at `;`); `I` rows are the interfaces this milestone fixes for downstream (roadmap §4.3 plan scope: "Extends `PageCommand` and `Intent`" + the interactable id scheme shared with 2A + the full FSM tree), each with a contract test written from the 2A consumer's side.

| # | Criterion (verbatim) | Kind | Check |
|---|---|---|---|
| E1 | Fitts page in Playwright meets the 4.6 targets for acquisition time and click precision | mechanical | `pnpm exec playwright test -c apps/extension/playwright.config.ts fitts.e2e.ts` |
| E2 | XState model tests pass | mechanical | `pnpm vitest run packages/gesture-core/test/model.test.ts` |
| E3 | Playwright kills the service worker and asserts recovery | mechanical | `pnpm exec playwright test -c apps/extension/playwright.config.ts sw-recovery.e2e.ts` |
| I1 | PageCommand (adds pointer/highlight/preview/snapshot/fallbackClick) | consumer:2A | `pnpm vitest run packages/protocol/test/contracts/2A-pagecommand.contract.test.ts` |
| I2 | PageEvent (adds hover/snapshot) | consumer:2A | `pnpm vitest run packages/protocol/test/contracts/2A-pageevent.contract.test.ts` |
| I3 | A11yItem interactable id scheme (shared with the 2A a11y snapshot) | consumer:2A | `pnpm vitest run packages/protocol/test/contracts/2A-a11yitem.contract.test.ts` |
| I4 | Intent (adds Click/DragStart/DragEnd/Swipe/HoldGesture) | consumer:2A | `pnpm vitest run packages/protocol/test/contracts/2A-intent.contract.test.ts` |
| I5 | FSM full Armed.* state tree (Pointing/PinchDown/Dragging/SwipeArmed/Hold) | consumer:2A | `pnpm vitest run packages/gesture-core/test/contracts/2A-fsm-state-tree.contract.test.ts` |

`I` contract tests are written from the 2A consumer's side (a11y snapshot + id scheme, highlight/preview candidates, `HoldGesture` triggering `Agent.*`, the state tree `Agent.*` attaches to), assert through the owning package's public export, fail today, and are made to pass by execute without being edited. All five fail at plan time (the schema members and FSM states do not exist yet); Tasks 1–2 make them pass.

## Status

_Owned by the 1C session; rewritten, not appended._

**Done (session 0, plan):**
- Read inputs: roadmap §4.3 + §8 (G5, G6-MOCK, 1A freeze), arch §3/§5/§6, `plans/README`, 1A plan + frozen contracts, current `protocol`/`gesture-core`/`content`/`background` source, tech-stack §4 tunables, spike §G5/§G6.
- Brainstorm → three owner questions batched (2026-09-12), answered A/A/A: Q1 CDP-when-granted + synthetic fallback; Q2 pointer via `PageCommand.pointer` SW-relay; Q3 back/forward via swipe, tab/zoom capability-only.
- Wrote `1C.spec.md`, `1C.impl.md` (8 tasks, each with exact Files + TDD steps), this plan, the Exit checks table, and **ADR 0001 (proposed)** for the pointer deviation (+ `docs/adr/README.md` row).
- Five 2A contract tests written under the owning packages' `test/contracts/` (I1–I5), all failing today.

**Done (execute session 1, Tasks 1–2):**
- **Task 1 `[protocol]`** (`6019fa9`): `PageCommand` += `pointer`/`highlight`/`preview`/`snapshot`/`fallbackClick`; `PageEvent` += `hover`/`snapshot`; `Intent` += `Click`/`DragStart`/`DragEnd`/`Swipe`/`HoldGesture`; new `A11yItem` (numeric id scheme) + `CursorState`. Frozen 2A I1–I4 + 1C contracts pass.
- **Task 2 `[gesture-core]`** (`6db31d4`): full `Armed.*` tree (`Pointing`/`PinchDown`/`Dragging`/`SwipeArmed`/`Hold` beside `Idle`/`Scrolling`), pinch-tap/dwell/drag/swipe/hold, stable-track gate, hold/swipe cooldowns, frame-driven scroll inertia. Frozen 2A I5 + 1C tree/replay contracts + E2 model tests pass. New timing constants in `constants.ts`; `FrameInput` extended (`pinch`/`pointer`/`hoverId`/`dwellEnabled`).
- Session decisions (CLAUDE.md §1, logged in `docs/sdd/1C/progress.md`): timing derived from `FrameInput.ts` deltas (not XState `after`, incompatible with the frozen synchronous `replayFrames` contract); scripted-frame replays (fixtures/ out of scope); `replay.ts` subscribes to the new intents.

**Done (execute session 2, Tasks 3–4):**
- **Task 3 `[page-index]`** (`79bb32d`): `createInteractableIndex` (WeakMap-stable numeric id scheme shared with 2A, `MutationObserver`/scroll/resize rebuild, visibility filter, same-origin iframe geometry offset, cross-origin frames contribute only the frame box), `SpatialGrid` (cell-bucketed broad-phase), `INTERACTABLE_SELECTOR`/`isVisible`/`roleOf`/`nameOf`. `at(x,y,radius?)` extended with an optional radius for snapping broad-phase. 9 tests pass (happy-dom).
- **Task 4 `[page-index]`** (`8b91fed`): `createSnapper` — speed-scaled radius (`snap` returns null above `SNAP_SPEED_CUTOFF`), nearest distance-to-bbox search over `index.at` candidates, neighbour hysteresis (single clean transition, no flicker — property test), pinch `latch()`/`release()`. Spatial tunables in `page-index/src/constants.ts`. 6 tests pass.
- Session decision (CLAUDE.md §1, not owner-level): the impl plan named the hysteresis tunable `SNAP_HYSTERESIS_PX`, but `boundary-lint` rule 4 rejects any `HYSTERESIS`-named const outside gesture-core (its keyword guards *gesture-timing* hysteresis). This is a *spatial px* margin, which roadmap §4.3 explicitly assigns to page-index, so the rule's intent is intact. Renamed the export to **`SNAP_NEIGHBOUR_MARGIN_PX`** (and the option to `neighbourMarginPx`) — Task 5 (content) imports the new name. The lint keyword would ideally be narrowed to distinguish spatial from timing hysteresis (owner note in handoff).

**Done (execute session 3, Task 5; Task 6 blocked):**
- **Task 5 `[content]`** (`6be45c3`): closed-shadow-root cursor overlay (`createCursorOverlay`: `aria-hidden`, `pointer-events:none`, top z-index, state class + `translate3d`, `highlight`/`preview` draws), pure `page-plane` (`handlePageCommand`: Zod-validate → `pointer` snap+overlay+`hover`, `snapshot` from `index.items()`, `highlight`/`preview`, `fallbackClick`→`syntheticClick`), and `synthetic-dispatch` (pointerdown→mousedown→pointerup→mouseup→click + focus, `dispatchEvent`-only fallback). Wired in `content/index.ts` keeping the 1A ready+scroll path; added `@gesture/page-index` as an extension dep. 10 unit tests pass; typecheck/lint/boundary-lint clean; `exit-check 1C --fast` → E2 + I1–I5 PASS.
- Session decisions (CLAUDE.md §1, in `docs/sdd/1C/progress.md`): pointer wire coords are **normalized [0,1]** (SW can't know viewport; content maps to px via live `window` — **Task 6 `relayPointer` must emit normalized coords**); overlay writes `transform` directly (relay already coalesces one msg/frame, ADR 0001 — single coalescing point); `fallbackClick` resolves id→Element via the public `items()`+`at()` API (no new page-index method).

**Done (execute session 3 continued, Task 6; ADR 0001 accepted):**
- **ADR 0001 accepted** (`fc24d4b`): owner approved the pointer-plane-via-SW-relay deviation at the plan gate; status flipped `proposed → accepted`.
- **Task 6 `[background]`** (`5cb4d52`): `pointer.ts` (`relayPointer` → `PageCommand.pointer` over the SW→CS port, normalized coords, one/frame; `cursorStateFor`), `cdp.ts` (`createCdp` per-session attach/detach, optional-`debugger` request on first need with synthetic fallback on denial, `trustedClick` = scroll-into-view then trusted press/release at the box centre, `trustedDrag`), `actions.ts` (`back`/`forward` bound to swipe in Standard; `selectTab`/`zoom` capability-only, no binding — Q3=A), extended `dispatcher.ts` (`dispatchIntent(intent, DispatchCtx)`: Click CDP-or-`fallbackClick`, Swipe→nav/off-in-Accessibility, Arm/Pause→attach/detach, HoldGesture→2A), `fsm.ts` (`toFrameInput` forwards `pinch`/`pointer`/`hoverId`/`dwellEnabled`; `createFrameConsumer` tracks FSM state + relays each frame), and `background.ts` wiring (real `chrome.*` behind injected interfaces; `lastHover` from `PageEvent.hover`). Session decisions in `docs/sdd/1C/progress.md` (tests consolidated in `apps/extension/test/`; 1A dispatcher/fsm-wiring tests updated for the extended signatures; CDP-only drag; `relayPointer` 4th hover arg). 21 affected-suite unit tests pass; typecheck/lint/boundary-lint clean; `exit-check 1C --fast` → E2 + I1–I5 PASS.

**Done (execute session 4, Tasks 7–8; via `obra-subagent-driven-development`):**
- **Task 7 `[background]`** (`ebafa02`): SW hardening. `ports.ts` — reconnect (re-register overwrites the tab entry), `SessionStore`-injected persistence of derived state (`contentTabs`/`cdpAttached`; live Ports can't serialize) + `restoreState()`. `reinject.ts` (pure/injectable) — `onNavigationCommitted` (re-inject when the content port is absent) + `reinjectMissing`. `background.ts` — wires `webNavigation`/`scripting` behind small interfaces and drives `restoreState()`→`reinjectMissing` at startup so a previously-connected tab recovers without a navigation; `webNavigation` added to manifest permissions. `ports.ts`/`reinject.ts` stay free of `chrome.*`/`wxt/browser`. 79 unit tests pass. (1 SDD fix round: the review caught `restoreState()`'s result being discarded — recovery was navigation-triggered only — and it was wired to proactive startup re-injection.)
- **Task 8 `[extension]`** (`a47da47`): e2e. `fitts.e2e.ts` (**E1 PASS** — 9/9 hits, precision 1.000, median acquisition 409 ms ≤ 1800), `dispatch-count.e2e.ts` (synthetic fallback lands `isTrusted===false`; the trusted-CDP branch is not forceable in Playwright headless so it is unit-covered per the plan — `cdp.test.ts`/`dispatcher.test.ts`; `isTrusted` never faked), `sw-recovery.e2e.ts` (**E3 exit-check PASS** via a content-plane-reload proxy). Fixtures + three playwright projects. `offscreen/main.ts`: authorized reconnect-on-disconnect (rate-limited, mutable `swPort`) + a `VITE_TEST_HOOKS`-gated live-frame suppression (a determinism hook — the hand-less fake camera streams ~16 fps `present:false, pinch:0` frames that otherwise compete with scripted frames; constant no-op in production). `scripts/milestone/exit-check 1C --fast` → **8 PASS 0 FAIL**, lock OK.

**Done (execute session 5, Task 9; via `obra-subagent-driven-development`):**
- **Task 9 `[docs]`** (`a9c46f0` + fix `c6841eb`): the owner-directed (2026-09-13 answer #4) arch-doc + rule update recording accepted **ADR 0001** (pointer plane via SW relay), per CLAUDE.md §3 step 4 / §5 (same PR as the deviation). `docs/02-architecture.md`: §2 diagram (dropped the direct `WK→CS PointerUpdate` edge; pointer rides `WK→SW` in `GestureFrame` + `SW→CS` as `PageCommand{pointer} @frame rate (ADR 0001)`), §3.1 pseudocode (no direct-to-CS pointer port; SW relays), §3.2 dispatcher prose (pointer routed via SW as `PageCommand{type:'pointer',x,y,state:CursorState}` over the frozen `ServiceWorkerToContent` port, one coalesced msg/frame; direct offscreen→CS `MessagePort` kept **reserved** with the MV3 rationale), §4.1 sequence diagram label canonicalized (`PointerUpdate{x,y}`→`PageCommand{pointer x,y}`). `.claude/rules/background.md` + `.claude/rules/protocol.md` gain one additive documentary bullet each. Arch §6/§8 untouched (already matched the accepted route); no code/schema/test change. One implementer + task review (spec + quality + boundary gate) + 1 fix round + scoped re-review; boundary-lint OK. Session rulings in `docs/sdd/1C/sdd-5-ledger.md`.

**E3 resolved (owner, 2026-09-13 answer #1 = A):** accept the content-plane-reload proxy e2e + unit coverage of `restoreState`/`reinjectMissing`/`swPort` reconnect; **E3 met**, with the limitation documented; a literal SW-kill e2e is a **1E** candidate. E1 thresholds were not weakened.

**Done (finish session 6):**
- Re-verified per CLAUDE.md §5: `pnpm typecheck` clean; `pnpm lint` clean + boundary-lint OK; `pnpm test` = extension 79 + playground 22 pass; `scripts/milestone/exit-check 1C` (full, clones for clean-clone rows) = **8 PASS 0 FAIL 0 OWNER**, lock OK (frozen at `640e4f7`). All three roadmap exit criteria met (E1 acquisition 409 ms ≤ 1800 / precision 1.000; E2; E3 via the owner-accepted content-plane-reload proxy).
- Finished the branch (`obra-finishing-a-development-branch`): all 9 tasks committed to `1C` (24 commits over `master`); opened PR to `master`. Did **not** merge and did **not** edit roadmap §8.

**Next:** milestone is ready to finish — the remaining work is **owner-only**: review + merge the 1C PR (which carries the ADR 0001 arch/rule doc update) and log the roadmap §8 rows below via `scripts/milestone/log-decision`. No further execute session is needed unless the PR review reopens something.

**Blockers:** none for the agent. All 9 tasks implemented and committed; all 8 exit-check rows PASS; PR open. Milestone exit is gated only on the owner's PR merge + §8 log (owner-scoped finish).

**Proposed decision(s) for roadmap §8 (owner logs; agent does not edit §8):**

> | 2026-09-12 | **1C page-plane interfaces = FIXED.** `protocol` additive extensions: `PageCommand` += `pointer`/`highlight`/`preview`/`snapshot`/`fallbackClick`, `PageEvent` += `hover`/`snapshot`, `Intent` += `Click`/`DragStart`/`DragEnd`/`Swipe`/`HoldGesture`, new `A11yItem` (numeric id scheme, stable per page load, shared with 2A) and `CursorState`. FSM `Armed.*` completed (`Pointing`/`PinchDown`/`Dragging`/`SwipeArmed`/`Hold`, arch §5); all timing/hysteresis/cooldown/dwell/scroll-inertia in `gesture-core` (single owner). Dispatch = CDP when `debugger` granted, synthetic content-script fallback otherwise (G5); attach per gesture-session while armed, detach on pause; CDP targets the element box after scroll-into-view (G5 caveat). Pointer plane = `PageCommand.pointer` over the SW→CS port (**ADR 0001**, direct offscreen→CS port infeasible in MV3). Swipe-left→back / swipe-right→forward (Standard profile; off in Accessibility); tab-switch + zoom dispatcher-capable, no gesture binding (bound in 1D.3). 1D/1E/2A extend, none redefines. | 1A merged; G5; G6 (MOCK); owner answers 2026-09-12 (spec §Owner decisions); ADR 0001 | Fixed in `docs/plans/1C.md`; consumed by 1D/1E/2A |

> | 2026-09-12 | **ADR 0001 (pointer plane via SW relay) = ACCEPTED.** The direct offscreen→content-script `PointerUpdate` port (arch §2/§3.2) is infeasible in MV3 (offscreen has no `chrome.tabs`; `MessagePort`s cannot cross `chrome.runtime` messaging). Pointer is relayed as `PageCommand{pointer}` over the frozen SW→CS port, one coalesced message/frame, within the arch §8 budget. On acceptance, update arch §3.2/§2 to describe the SW relay and keep the direct port as a reserved perf optimization. | ADR 0001; MV3 API limit; owner review | `docs/adr/0001-pointer-plane-via-sw-relay.md` → `accepted`; arch §3.2/§2 updated in the same PR |
