# Milestone 1D.5 — 1D.5-diagnostics

| | |
|---|---|
| Plan | `docs/plans/1D.5-diagnostics.md` |
| Branch | `1D.5` from `master` @ `21331fa` |
| Roadmap | `docs/05-roadmap.md §4.4` |
| PR | https://github.com/duylongpro99/gesture2browse/pull/10 |
| Updated | 2026-09-14 @ `2743504` by `scripts/milestone/journal 1D.5` |

## Verification

Run from the repo root, on branch `1D.5` or on `master` after the merge. `scripts/milestone/journal 1D.5 --recheck` runs every command below and appends a line to Runs; the `owner` rows are the numbered steps under Owner steps. The table is the plan's frozen `## Exit checks` (`docs/plans/1D.5-diagnostics.md`); a change to it is a re-plan, never an edit here.

| # | Criterion | Kind | Check | Last result |
|---|---|---|---|---|
| E1 | Playwright screenshot reviewed by the owner | owner | steps below | owner: steps below |
| E2 | settings round-trip through `chrome.storage` validated by the `protocol` schema | mechanical | `pnpm exec playwright test -c apps/extension/playwright.config.ts diagnostics.e2e.ts` | PASS 2026-09-14 @ 2743504 (recheck) |
| I1 | PumpStat (adds per-stage timings + dropped-frame count, additive) | consumer:1E | `pnpm vitest run packages/protocol/test/contracts/1E-pumpstat-stages.contract.test.ts` | PASS 2026-09-14 @ 2743504 (recheck) |
| I2 | FalsePositiveEntry (owner-annotated; feature window always, landmark window when armed) | consumer:1E | `pnpm vitest run packages/protocol/test/contracts/1E-false-positive.contract.test.ts` | PASS 2026-09-14 @ 2743504 (recheck) |
| I3 | DiagnosticsExport replayable against fixtures via gesture-core | consumer:1E | `pnpm vitest run packages/gesture-core/test/contracts/1E-diagnostics-export-replay.contract.test.ts` | PASS 2026-09-14 @ 2743504 (recheck) |

### Owner steps
<!-- Written by the worker that reaches an owner row (execute brief, step 4); kept by scripts/milestone/journal. -->
<!-- One block per owner row: a line **E3 — <criterion>**, then the numbered steps the owner runs, then a line starting with Expected: -->

**E1 — Playwright screenshot reviewed by the owner**
1. From the repo root on branch `1D.5` (or on `master` after the merge), run
   `pnpm exec playwright test -c apps/extension/playwright.config.ts diagnostics.e2e.ts`.
   It builds the extension and, with the y4m fake webcam, drives the diagnostics page.
2. Open the artifact it wrote: `apps/extension/test-results/diagnostics.png` (full-page
   capture of `diagnostics.html`).
3. Confirm the page shows: an fps readout (`28.5 fps`), a `dropped: 3` count, the five
   per-stage timing rows (capture/infer/normalize/classify/filter in ms), a transition
   stream (2 entries), and one false-positive row that names its frame count and
   `2 landmark frames`. Confirm **no raw video / camera image** is rendered anywhere on
   the page — numbers and a landmark-frame count only.

Expected: the diagnostics page renders every readout legibly from the seeded snapshot and
shows no raw video; owner marks E1 met (`scripts/milestone/log-decision`/roadmap §8 owner step).

### Runs
<!-- appended by scripts/milestone/journal: every full exit-check run (finish) and every --recheck -->
- 2026-09-14 recheck @ 2743504 on 1D.5: 4 PASS 0 FAIL 1 OWNER 0 other

## Implemented

Branch `1D.5` from `master` @ `21331fa` to `2743504`: 18 commits, 37 files changed, 2590 insertions(+), 22 deletions(-).

### Session-end writes (`[docs]`)
- `aa57474` [docs] task plan: 1D.5 diagnostics spec + impl plan (`docs/plans/1D.5-diagnostics.impl.md`, `docs/plans/1D.5-diagnostics.spec.md`)
- `99615a9` [docs] task workspace: 1D.5 SDD progress ledger (`docs/sdd/1D.5-diagnostics/progress.md`)
- `bcbb434` [docs] session end: 1D.5 plan doc + STATUS row (journal blocked by kit bug) (`docs/STATUS.md`, `docs/plans/1D.5-diagnostics.md`)
- `127d316` [docs] plan 1D.5: milestone journal (`docs/journal/1D.5-diagnostics.md`)
- `0d587ee` [docs] session end: 1D.5 execute s1 (Tasks 1–2, I1/I2 green) (`docs/STATUS.md`, `docs/journal/1D.5-diagnostics.md`, `docs/plans/1D.5-diagnostics.md`, `docs/sdd/1D.5/progress.md`)
- `9c2996a` [docs] session end: 1D.5 execute s2 (Tasks 3–4) (`docs/STATUS.md`, `docs/journal/1D.5-diagnostics.md`, `docs/plans/1D.5-diagnostics.md`, `docs/sdd/1D.5/progress.md`)
- `f2ef5f4` [docs] session end: 1D.5 execute s2 journal (session-2 row) (`docs/journal/1D.5-diagnostics.md`, `docs/plans/1D.5-diagnostics.md`)
- `5341f8c` [docs] session end: 1D.5 execute s3 (Tasks 5–6, I3 green) (`docs/STATUS.md`, `docs/journal/1D.5-diagnostics.md`, `docs/plans/1D.5-diagnostics.md`, `docs/sdd/1D.5/progress.md`)
- `2743504` [docs] session end: 1D.5 execute s4 (Task 7, E2 green; E1 owner-gated) (`docs/STATUS.md`, `docs/journal/1D.5-diagnostics.md`, `docs/plans/1D.5-diagnostics.md`, `docs/sdd/1D.5/progress.md`)

### Other commits (no `[component] task N` line)
- `da6a8c6` [protocol] task exit-checks: 1D.5 I1/I2 contract tests (fail today) (`packages/protocol/test/contracts/1E-false-positive.contract.test.ts`, `packages/protocol/test/contracts/1E-pumpstat-stages.contract.test.ts`)
- `e504603` [gesture-core] task exit-checks: 1D.5 I3 contract test (fail today) (`packages/gesture-core/test/contracts/1E-diagnostics-export-replay.contract.test.ts`)
- `1b36df5` [protocol] task 1: diagnostics shapes (additive) (`packages/protocol/src/diagnostics.ts`, `packages/protocol/src/index.ts`, `packages/protocol/src/pump.ts`, `packages/protocol/test/diagnostics.test.ts`)
- `9982f70` [offscreen] task 2: emit per-stage timings + dropped-frame count (`apps/extension/entrypoints/offscreen/gesture-frame.ts`, `apps/extension/entrypoints/offscreen/inference.worker.ts`, `apps/extension/entrypoints/offscreen/main.ts`, `apps/extension/entrypoints/offscreen/stage-timer.ts`, `apps/extension/test/stage-timer.test.ts`)
- `3f3d401` [offscreen] task 3: rolling landmark buffer (record-landmarks, default OFF) (`apps/extension/entrypoints/offscreen/inference.worker.ts`, `apps/extension/entrypoints/offscreen/landmark-buffer.ts`, `apps/extension/entrypoints/offscreen/main.ts`, `apps/extension/test/landmark-buffer.test.ts`)
- `2341f65` [background] task 4: feature window, false-positive builder, record relay (`apps/extension/entrypoints/background.ts`, `apps/extension/entrypoints/background/diagnostics.ts`, `apps/extension/test/diagnostics-sw.test.ts`)
- `34c18ed` [gesture-core] task 5: DiagnosticsExport → replay conversion (`packages/gesture-core/src/diagnostics-replay.ts`, `packages/gesture-core/src/index.ts`, `packages/gesture-core/test/diagnostics-replay.test.ts`)
- `a8d0302` [diagnostics] task 6: diagnostics page (new entrypoint) + rule file (`.claude/rules/diagnostics.md`, `apps/extension/entrypoints/diagnostics/App.tsx`, `apps/extension/entrypoints/diagnostics/export.ts`, `apps/extension/entrypoints/diagnostics/index.html`, `apps/extension/entrypoints/diagnostics/main.tsx`, `apps/extension/entrypoints/diagnostics/view.ts`, …)
- `48ba621` [diagnostics] task 7: e2e — render, annotate, config round-trip, export→replay, screenshot (`apps/extension/playwright.config.ts`, `apps/extension/test/diagnostics.e2e.ts`)

## Sessions

| N | Role | Recorded | Outcome | Summary |
|---|---|---|---|---|
| 0 | plan | 2026-09-14 | DONE | Plan complete — spec, impl plan (7 tasks with Files: blocks), five-question plan doc, frozen ## Exit checks + three failing consumer-side contract tests, SDD workspace/ledger, and the milestone journal; owner design gate resolved (Q1=A live producer, Q2=A owner-annotated false-positive log, Q3=C record-landmarks toggle default off). |
| 1 | execute | 2026-09-14 | CONTINUE | Landed Task 1 (protocol diagnostics shapes; PumpStat += optional stages/dropped) and Task 2 (offscreen per-stage timings + dropped-frame count); I1/I2 contract tests now PASS. |
| 2 | execute | 2026-09-14 | CONTINUE | Offscreen rolling landmark buffer (default OFF) and the SW diagnostics recorder — feature/landmark windows, false-positive builder, record relay — landed (Tasks 3–4). |
| 3 | execute | 2026-09-14 | CONTINUE | Tasks 5 & 6 done — gesture-core export→replay helpers (I3 green) and the WXT-discovered diagnostics React page + rule file. |
| 4 | execute | 2026-09-14 | NEEDS-OWNER | Task 7 (last task) landed — diagnostics.e2e.ts turns E2 green and captures the E1 screenshot; all 7 impl tasks DONE, only the owner's E1 screenshot review remains. |
| 5 | finish | 2026-09-14 | DONE | Verified 1D.5 (§5 green; exit-check 4 PASS 0 FAIL 1 OWNER, lock OK; E1 owner-approved), pushed branch, opened PR #10 to master. |

## Decisions

Proposed for roadmap §8 (the owner logs it; `scripts/milestone/log-decision 1D.5 --apply` after the merge):

**Proposed decision(s) for roadmap §8 (owner logs; agent does not edit §8):**

ADRs added on this branch: none.

## Notes
<!-- Free text by the worker (what to know that git and the tables do not say); kept by scripts/milestone/journal. -->

**Journal tooling note (session 2).** The worktree's own `scripts/milestone/journal 1D.5`
(unpatched on branch `1D.5`) aborts (exit 1) for 1D.5: under `set -euo pipefail` its
`title=$(grep -E "… Milestone 1D\.5 …" docs/05-roadmap.md | head | sed)` returns non-zero
because 1D.5 has no dedicated roadmap heading — it lives under the shared `### 4.4
Milestones 1D.1–1D.6` §4.4 row — so the empty grep kills the run before assembly. This
journal was regenerated with the PATCHED root copy at
`/Users/onedayin20902/personal/human-gesture/scripts/milestone/journal` (fix is on master,
not merged onto `1D.5`; scope forbids editing `scripts/`). Future sessions on this branch:
run the root copy, not the worktree's.

**Execute session 2 (2026-09-14) — CONTINUE:**
Landed Task 3 (`3f3d401`) offscreen rolling landmark buffer (`LandmarkBuffer`, default OFF;
worker attaches `landmarks` only while armed; `main.ts` relays `SetRecordLandmarks` and
re-arms across pump restarts) and Task 4 (`2341f65`) SW `DiagnosticsRecorder` (bounded
`FrameSample` window always + `FixtureFrame` window when armed; `buildFalsePositive`
snapshots them plus the last transition; SW persists a bounded false-positive
`storage.session` series and persists+relays `DiagnosticsConfig`). `tsc`/lint/boundary-lint
green; 94 unit tests pass. Exit checks unchanged: I1/I2 PASS, I3 FAIL (Task 5, next), E1/E2
pending (Tasks 6–7). Note: the extension never imports `zod` directly (all Zod lives in
`packages/protocol`), so the `FlagFalsePositive` envelope uses a pure type-guard and
`SetRecordLandmarks`'s config is validated with the protocol `DiagnosticsConfigSchema`.
