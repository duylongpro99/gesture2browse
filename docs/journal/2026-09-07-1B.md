# 2026-09-07 — 1B perception pipeline — session 0 (plan)

## What happened
Planned milestone 1B. Brainstormed (architectural path) and batched three owner questions via a NEEDS-OWNER handoff; the owner answered all three option (a). Then wrote the spec, implementation plan, plan (five questions + Exit checks), and seeded the SDD ledger.

## Owner decisions (2026-09-07, `docs/sdd/1B/driver.json`)
- **Q1 — G3/G8 scope:** plan 1B for the **browser-inference path only**. G1 is GO at 30 fps and the WebGL→WASM delegate fallback is existing architecture. Delegate order is a bench tunable; the final G8 GO/NO-GO is owner sign-off deferred until G3 lands; the ONNX-Web fallback is a roadmap §9 re-plan trigger (Intel Air < 20 fps on every path), not a 1B component.
- **Q2 — classifier:** small **MLP trained offline** by a Node script (`scripts/train/`) over `fixtures/gestures/**`, emitting a weights JSON the offscreen worker loads at startup; keep the `Classifier` interface and `KnnClassifier` as the conformance fallback; in-browser retraining deferred to Phase 3 (track 3.1).
- **Q3 — vocabulary + mock:** classifier output vocabulary **is** the frozen `GestureLabel` enum; the MOCK role-names are FSM/Intent mappings, not labels (no new protocol labels, no `GestureFrame` label-set change). Proceed planning/executing on the MOCK G4 to test-drive the flow, with Exit E1 (precision/recall ≥ 95 %) treated as owner-verified/deferred (cannot pass mechanically without real fixtures).

## Design decisions recorded
- One protocol change: additive optional `GestureFrame.palmFacing` (perception → FSM/diagnostics). Additive keeps the frozen `gestureframe-v0` contract passing.
- Boundary tension resolved (CLAUDE.md §6): arch §3.1 puts the 3-frame vote + palm-facing gate in the offscreen classifier, but CLAUDE.md §2 requires all gesture timing/gating in the FSM. **CLAUDE.md wins** — palm-facing *boolean* offscreen (per-frame geometry), *vote + gate* in the FSM, made backward-compatible (`palmFacing===false` blocks, `undefined` passes) so the frozen `1C-fsm-state-tree` contract is preserved.
- No new runtime dependency (pure-TS MLP); `scripts/train/` under the allowed `scripts/`; no ADR needed.
- Exit checks: E1 owner (deferred on MOCK), E2 golden replay suite (mechanical), E3 adaptive-fps + context-loss Playwright (mechanical). No I-rows (roadmap row has no "Interfaces fixed here").

## Notes for next session (execute)
- Run `1B.impl.md` Tasks 1–7 in dependency order (see `docs/sdd/1B/progress.md`).
- This worktree had no `node_modules` at plan time; execute must `pnpm install` before the mechanical Exit checks resolve (`pnpm vitest run` / `pnpm exec playwright`, matching 1A's form).
- The plan `## Status` carries the proposed roadmap §8 row for the owner to log post-merge via `log-decision`.
