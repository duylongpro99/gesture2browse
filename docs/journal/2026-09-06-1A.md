# 2026-09-06 — Milestone 1A — session 0 (plan)

Worktree `.worktrees/1A`, branch `1A`, base `master`. Role: plan (produce spec, impl plan, plan, Exit checks, contract tests, SDD workspace). Plan inputs all in roadmap §8: G1 = GO, G5 = CDP, G8 provisional = GO (unblocks 1A only).

## What happened

- **Brainstorm (architectural path).** 1A freezes interfaces four downstream milestones depend on, so it took the full path. Settled the doc-decided parts (Intent stays `Arm/Pause/Scroll{dy}`, additive; `dy` = signed CSS px; `GestureFrame` unchanged; `PageEvent` = `ready` only; FSM hierarchical with arch §5 names) and batched four genuine owner questions via a `NEEDS-OWNER` handoff (commit `ca452b5`, spec draft).
- **Owner answers (2026-09-06), all as recommended:**
  1. **Scroll dispatch = content-script `scrollBy`** via `PageCommand{type:'scroll',dy}` (arch §3.2; no `debugger` in the slice). CDP stays the *click* default (1C). Roadmap §4.1's "G5 default path" was the loose phrasing; arch §3.2 governs scroll.
  2. **Direct offscreen→content-script port deferred to 1C.** Offscreen has no `chrome.tabs` (confirmed: current offscreen uses only `browser.runtime`), so a direct port needs a SW-brokered `MessageChannel`, and 1A's scroll slice has no cursor consumer. 1A wires offscreen→SW + SW→CS and reserves+documents the direct port (`PortName.OffscreenToContent`); live `PointerUpdate` wiring is 1C's, ADR then if brokering is infeasible. Avoids dead code.
  3. **FSM:** implement `Paused` + `Armed.{Idle,Scrolling}`; freeze arch §5 PascalCase hierarchical names as the additive target (no empty placeholder states). Transition-log shape `{ ts, from, to, event, intent? }` confirmed.
  4. **e2e determinism** (no G4 recordings; kNN placeholder can't classify fist from a synthetic y4m): fake camera runs the real pump; a test-only hook (gated by `VITE_TEST_HOOKS`, inert in production) publishes a scripted `GestureFrame` at the offscreen→SW boundary; e2e proves `Intent`→dispatch→`scrollBy`.
- **Planning artifacts** — finalized `1A-vertical-slice.spec.md`; wrote `1A-vertical-slice.impl.md` (6 tasks, exact Files blocks, Global Constraints naming the six owning rule files); `1A-vertical-slice.md` (five-question record + frozen Exit checks E1–E3 / I1–I5 + Status with the proposed §8 row); four contract tests under `packages/{protocol,gesture-core}/test/contracts/`; SDD workspace `docs/sdd/1A-vertical-slice/` with `progress.md`.

## Decisions / notes

- **Intent & GestureFrame are already at final schema.** `GestureFrame` was frozen in 0A; `Intent`'s provisional schema equals its final shape (1A only lifts the "provisional" marker). Their `I`-row contract tests (I1, I2) are therefore **green at plan time** and act as freeze guards; only I3/I4/I5 (`PageCommand`, `PageEvent`, FSM tree + `TransitionLogEntry`) fail today and are made to pass by execute. Recorded explicitly in the plan and spec so this is not mistaken for a broken "fails-today" rule.
- **Transition-log ownership.** The log *shape* lives in `protocol` (`TransitionLogEntry`); its *derivation* stays in `gesture-core` (a `createGestureRunner` reused by both the replay tests and the live SW), keeping one owner for timing + log per CLAUDE.md §2.
- **Fixture-replay limitation.** 1A keeps the untrained kNN placeholder and a `FixtureRecord` carries one label per record, so a palm→fist *sequence* can't come from a raw-landmark fixture; E1 drives scripted `FrameInput` through the FSM. Real-landmark classifier replay is 1B.
- **Scope.** This session wrote only `docs/**` and `packages/*/test/contracts/**` (the two contract-test paths). No source/config touched — all code is in the `.impl.md` for execute.

## Handoff

`outcome: DONE`. Commits this session: `ca452b5` (spec draft, NEEDS-OWNER round) + the finalize/tests/ledger/session-end commits below. Next: the driver shows the Exit table + contract tests to the owner and freezes (`exit-check 1A --freeze`), then execute runs Task 1 (protocol) → Task 6 (e2e), one component per session, `exit-check 1A --fast` after each.

---

# 2026-09-06 — Milestone 1A — session 1 (execute, Tasks 1–2)

Worktree `.worktrees/1A`, branch `1A`, base `76503d9`. Role: execute via `obra-subagent-driven-development`; owns Tasks 1 (`protocol`) and 2 (`gesture-core`) per `.claude/scope.json`. SDD workspace `docs/sdd/1A-vertical-slice/`; ledger `docs/sdd/1A/progress.md`.

## What happened

- **Preflight scan** (ledger): Task 1→2 is a clean sequential interface dependency (Task 2 imports `TransitionLogEntry`/`Intent` from Task 1's `@gesture/protocol`), no shared files; the only self-consistency note was that compound `Armed` breaks existing `machine.test.ts` `value==='Armed'` assertions — expected, the brief lists that file as one to modify. Scan clean.
- **Task 1 `[protocol]` (commit `17a4b87`).** Added `page-command.ts`/`page-event.ts`/`transition.ts`/`ports.ts`, lifted Intent's PROVISIONAL marker (schema unchanged), exported all four. 31 unit+contract tests green, typecheck clean. Task review: Spec ✅ / Quality ✅, no findings.
- **Task 2 `[gesture-core]` (commits `1afe418..48481dc`).** `Armed` → compound `{Idle,Scrolling}` with a new `fistReleased` guard; parent `Armed` keeps the clutch catch-all so unmatched child frames bubble (Pause-from-Armed preserved). Scroll `dy = Math.round(vy * SCROLL_PX_PER_UNIT)` (new `constants.ts` tunable, v0 = 400, signed CSS px). New `createGestureRunner()`/`replayFrames()` produce the frozen `TransitionLogEntry` shape with dotted state paths. Contract I5 + E1 `replay-scroll.test.ts` green.
- **Task 2 review found one Critical → fix round 1.** First implementation had `createGestureRunner().send()` return intents/transitions accumulated across ALL calls; the brief's contract ("return the intents emitted" per send) and the named downstream consumer (Task 5 SW live log reuses `createGestureRunner`) require a per-frame delta — cumulative return would grow unboundedly and push log-diffing onto the consumer. Fix: `send()` returns that frame's delta only; `replayFrames()` owns accumulation; +2 tests asserting the per-frame contract. Re-review: ADDRESSED, no new breakage. 21/21 gesture-core tests pass.

## Decisions / notes

- **Ruling (Task 2 Critical, agreed with reviewer):** `send()` = per-frame delta, not cumulative. Cost if wrong: a small follow-up in Task 5; but the brief wording and the Task 5 consumer both point per-frame, and `replayFrames`' external result is identical either way, so risk is low.
- **`SCROLL_PX_PER_UNIT` is a fixture-tunable placeholder (400)**, not a plan constant; E1's scripted replay is the fixture replay that covers it (1A keeps the untrained kNN, so scripted `FrameInput`, not raw-landmark fixtures — per spec §5).
- **Deferred minor** (for the Task 6 final whole-branch review): `replay.ts` attaches only `emitted[0]` to a `TransitionLogEntry` — matches the brief's singular `intent?`; revisit only if a future machine adds multi-intent transitions.
- **dist gotcha:** `@gesture/protocol` and `@gesture/gesture-core` resolve through `dist/` via `package.json` `exports`; consumers/tests must rebuild after src changes. Carried between tasks; Tasks 3–5 (extension imports both) should heed it.
- **Scope.** Session wrote only `packages/protocol/**`, `packages/gesture-core/**` (non-contract), and `docs/**`. No contract test edited; no config touched.

## Handoff

`outcome: CONTINUE`. `exit-check 1A --fast` @ `48481dc`: 7 PASS (E1, E3, I1–I5), 1 FAIL (E2 — scroll e2e absent until Task 6), lock OK. Next session runs Task 3 (`content` — execute scroll `PageCommand`, announce ready), then 4→6, one component per session.

---

# 2026-09-06 — Milestone 1A — session 2 (execute, reconcile Tasks 1–2)

Worktree `.worktrees/1A`, branch `1A`, base `75193c0`. Role: execute; scope owns Tasks 1–2. Per owner answer #2 (session-1 handoff), this session reconciles the ledger: verify the committed Tasks 1–2 rather than start new code (both were already implemented, reviewed, and marked done in session 1's commit `75193c0`).

## What happened

- **Verified committed Tasks 1–2.** Clean tree at `75193c0`; Task 1 (`17a4b87`) and Task 2 (`1afe418..48481dc`) present. `pnpm typecheck` clean for `@gesture/protocol` + `@gesture/gesture-core`; `pnpm vitest run` = 67/67 green (schemas, all five contract tests I1–I5, E1 `replay-scroll`, FSM/normalize/one-euro/roundtrip).
- **`exit-check 1A --fast` @ `75193c0`:** 7 PASS (E1, E3, I1–I5), 1 FAIL (E2 — `scroll-slice.e2e.ts` not created until Task 6), lock OK (frozen at `76503d9`). Unchanged from session 1 — no regression.
- **Ledger already reconciled.** `progress.md` and the plan `## Status` already record Tasks 1–2 complete (written in `75193c0`); no ledger edit was needed beyond appending this session's verification note.

## Decisions / notes

- Nothing to build this session: scope's two tasks were done. Did not touch Task 3+ (not in scope; session 3 owns `content`). No source/config/contract-test changes.

## Handoff

`outcome: CONTINUE`. Tasks 1–2 verified green at `75193c0`. Next session runs Task 3 (`content` — execute scroll `PageCommand` via `scrollBy`, announce `ready` `PageEvent`), then 4→6, `exit-check 1A --fast` after each; E2 goes green at Task 6.

---

# 2026-09-06 — Milestone 1A — session 3 (execute, Tasks 3–4)

Worktree `.worktrees/1A`, branch `1A`, base `8fd7727`. Role: execute; scope owns Tasks 3 (`content`) and 4 (`offscreen`). Ran via `obra-subagent-driven-development`: one implementer subagent per task, a spec+quality task review after each, `exit-check 1A --fast` after each. All model dispatches on sonnet.

## What happened

- **Preflight scan (Tasks 3–4):** disjoint files (`content/**` vs `offscreen/**`), both consuming the frozen `protocol` (Task 1) and `PortName`; Task 4 also consumes `gesture-core` (Task 2). No cross-task contradiction; consistent with the Task 5 consumer's port directions (content/offscreen CONNECT, SW accepts). Ruling recorded for Task 3 testability (extract a pure `content/scroll.ts` so the happy-dom unit test avoids the `defineContentScript` wxt global).
- **Task 3 `[content]` (ddf67e6):** connects `ServiceWorkerToContent`, posts `PageEvent{ready,frameId:0}`, validates every inbound `PageCommand` with Zod before `window.scrollBy({top:dy})` (invalid ignored). 5/5 happy-dom tests, boundary-lint OK. Review: Spec ✅ / quality Approved, 2 deferred minors.
- **Task 4 `[offscreen]` (fe11f4b):** added `@gesture/gesture-core` to the extension; new `gesture-frame.ts` `createGestureFrameSource()` composes normalize→1€ filter→features→classifier into a schema-valid `GestureFrame` with `landmarks` omitted; the worker flattens hand[0]→`number[63]` locally and posts only the derived frame; `main.ts` opens `OffscreenToServiceWorker`, relays frames, and adds the fully-`VITE_TEST_HOOKS`-gated `__inject_frames` hook. 3/3 new + 20/20 extension suite, boundary-lint OK, tsc clean. Review: Spec ✅ / quality Approved, 3 deferred minors.
- **`exit-check 1A --fast` @ fe11f4b:** 7 PASS (E1, E3, I1–I5), 1 FAIL (E2 — `scroll-slice.e2e.ts` exists only at Task 6), lock OK. E3 boundary lint now passes with content + offscreen wired.

## Decisions / notes

- **Ruling (Task 3 testability):** pure logic in a sibling `content/scroll.ts`; `index.ts` stays a thin wxt-glue wrapper. Cost if wrong: one small file to fold back. No other rulings needed — no ADR, no rule deviation, no owner decision.
- 5 deferred minors (no Critical/Important across both tasks) recorded in `docs/sdd/1A/progress.md` and carried to the final whole-branch review at Task 6: redundant pre-normalization before `pinchDistance`/`fingerExtension`; velocity-across-a-gap approximation; uncalibrated image-space pointer; scrollBy `top`-only; and manifest-level port wiring unverified until the Task 6 e2e.
- Boundaries respected: content depends only on `protocol` + DOM + `browser.runtime`; offscreen keeps video/landmarks in-document (only the landmarks-less `GestureFrame` crosses the port); no gesture-timing constant left either component (1€ cutoffs are perception-smoothing, commented as such).

## Handoff

`outcome: CONTINUE`. Tasks 3–4 done + reviewed clean at `fe11f4b`. Next session runs Task 5 (`background` — consume `GestureFrame`, run the FSM via `createGestureRunner`, map `Scroll`→`PageCommand`, persist the transition log to `storage.session`), then Task 6 (Playwright fake-camera e2e; E2 goes green there). `exit-check 1A --fast` after each.

---

# 2026-09-06 — Milestone 1A — session 4 (execute, Tasks 5–6 — final)

Worktree `.worktrees/1A`, branch `1A`, base `eb621c2`. Role: execute; scope owns Tasks 5 (`background`) and 6 (`extension` e2e). Ran via `obra-subagent-driven-development`: one implementer subagent per task, a spec+quality+boundary-gate task review after each, `exit-check 1A --fast` after each, then a final whole-branch review. Implementers/reviewers on sonnet; final review on opus.

## What happened

- **Preflight scan (Tasks 5–6):** clean. Confirmed both port directions agree with the Task 3/4 producers (offscreen/content CONNECT, SW accepts + validates); `GestureFrame` is a superset of `FrameInput` (subset map); Task 6 is end-to-end over the wired slice (sequential 5→6). Ruling: extract the three sibling modules the plan prescribes so `background.ts` stays additive and vitest-testable without `chrome.*`; Ruling: the e2e imports timing constants from `@gesture/gesture-core` rather than hardcoding them.
- **Task 5 `[background]` (ebf9db0):** `onConnect` dispatches by `port.name`, validates every inbound message (`GestureFrameSchema`/`PageEventSchema` safeParse) before acting, runs the FSM via `createGestureRunner`, maps `Scroll`→`PageCommand` to the content port, and persists `TransitionLogEntry[]` to a bounded `storage.session` series (reused the existing `MAX_SERIES` idiom). Pure logic in `background/{fsm,dispatcher,ports}.ts`; `background.ts` change is strictly additive (camera/pump untouched). 7/7 focused + 27/27 suite, tsc clean, boundary-lint OK. Review: Spec ✅ / Boundary gate PASS / Approved; 1 ⚠️ controller-resolved as a non-gap (validate-before-use with no `ready` consumer yet is the 1A scope); 2 deferred minors.
- **Task 6 `[extension]` (c708f42):** `scroll-slice.e2e.ts` builds with `VITE_TEST_HOOKS=1`, launches a fake-camera context, serves a tall fixture over a node `http` server, injects a scripted palm-hold→fist `GestureFrame` sequence at the offscreen `__inject_frames` hook, and asserts the page scrolled (`scrollY` reached 240). `playwright.config.ts` gains the `scroll-slice` project. Review: Spec ✅ / Boundary gate PASS / Approved; 1 parked (shared `.output` build dir — not a diff defect); 1 deferred minor.
- **`exit-check 1A --fast` @ c708f42: 8 PASS / 0 FAIL** (E1, E2, E3, I1–I5), lock OK — **E2 green**.
- **Final whole-branch review (opus): Ready to merge — Yes.** Verified by direct inspection: seam coherence both directions; `gesture` always a valid `GestureLabel` (`'none'` fallback in-enum) so no valid frame is dropped at validation; video/landmark containment (worker flattens + `frame.close()`s, posts only landmarks-less `GestureFrame`); single-owner timing (no gesture-timing constant in offscreen/background/content production code). All 9 logged deferred minors triaged **correctly deferred**; 4 new cosmetic minors logged for 1B/1C.

## Decisions / rulings

- **Ruling (Task 5 testability):** three sibling modules with injected side effects (port-target + `persist`) → tests need no `chrome.*`. Cost if wrong: a small module seam to refold.
- **Ruling (Task 6 timing single-source):** the e2e imports `PALM_CLUTCH_MS`/`SCROLL_STEP`/`SCROLL_PX_PER_UNIT`/`MIN_CONFIDENCE` from `gesture-core`; no timing literal duplicated (CLAUDE.md §2). Cost if wrong: none — it's the correct single source.
- **Ruling (Task 5 ⚠️ resolved):** `PageEventSchema.safeParse` result discarded is NOT a gap — the brief says "validate before use" and 1A has no `ready` consumer; validation-with-no-consumer is the intended scope. Cost if wrong: a future `ready`-gated dispatch would need wiring — additive, no rework.
- **Ruling (Task 6 shared-`.output` parked):** the review's one "Important" is not a defect in the diff (reviewer concurs), does not affect the frozen E2 check (runs `scroll-slice` in isolation), and is contingent on future config outside the diff. Cost if wrong: a future combined run with a `*_SKIP_BUILD` env could load a test-hooks build into frame-pump — caught when such a run is added, fixed then with a distinct outDir.
- **Ruling (bookkeeping):** worker writes the prose ledger (`docs/sdd/1A/progress.md`); the slug-path table ledger's State/Commit for Tasks 5–6 is left to the driver's standing authorization (session-3 answer #1).
- No ADR, no rule deviation, no owner decision needed this session.

## Handoff

`outcome: DONE`. All six 1A tasks done + reviewed clean; final whole-branch review clean (Ready to merge — Yes); 8/8 exit-checks PASS at `c708f42`, lock OK. Execute phase complete. Next step is owner/driver, not the worker: open the 1A PR and merge to `master`, then log the roadmap §8 GO row (the frozen-interfaces block in the plan `## Status`) and remove the 1A STATUS row. The worker does not merge or edit §8; the SDD workspace is retained until merge.

---

# 2026-09-06 — Milestone 1A — session 5 (finish)

Worktree `.worktrees/1A`, branch `1A`, base `4dd4abe`. Role: finish — verify the milestone, finish the branch, open the PR to `master`. Never merges, never edits §8.

## What happened

- **Verification (`CLAUDE.md §5`):** `pnpm typecheck` 7/7 successful; `node scripts/lint/boundary-lint.mjs` OK (no trust-boundary violations); `pnpm test` all suites green (gesture-core 21/21, playground 22/22, plus protocol/extension). Working tree clean at `4dd4abe`.
- **Authoritative exit-check (`scripts/milestone/exit-check 1A`, full):** **8 PASS / 0 FAIL / 0 OWNER**, lock OK (frozen at `76503d9`), no `TAMPERED`. `summary: 8 PASS 0 FAIL 0 OWNER 0 other | agree 0 disagree 0 at-risk 0 missing 0 unmatched 0 | lock OK`.
- **Exit criteria (roadmap §4.1 Exit cell), each met:** fixture replay produces the expected `Intent` sequence (E1 PASS); Playwright with a fake camera scrolls a test page (E2 PASS); boundary lint passes on all three components (E3 PASS). Interfaces fixed here (I1–I5) all PASS — each consumable as-is by its named downstream (1B/1C/2A).
- **Branch finished:** committed the session-end doc writes, pushed `1A` to `origin`, opened the PR to `master` with `gh`. Did not merge.

## Decisions / notes

- Nothing to fix: finish role holds no code paths (scope `allow` is docs-only), and every check passed, so there was nothing to route around. The proposed §8 GO row (frozen-interfaces block) stands unchanged in the plan `## Status` for the owner to log on merge.

## Handoff

`outcome: DONE`. PR opened to `master`; 8/8 exit-checks PASS at `4dd4abe`, lock OK. Next: owner reviews and merges the PR, then logs the roadmap §8 GO row (the frozen-interfaces block in the plan `## Status`) via `scripts/milestone/log-decision` and removes the 1A STATUS row.
