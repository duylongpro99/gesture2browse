# 2026-09-06 — 0E (G7 agent latency probe)

## Session 0 — plan (docs-only)

First probe session; `.claude/scope.json` has no code paths, so per the brief this session writes the plan only and hands off `CONTINUE` (the next session's scope is derived from the plan's `**Files:**` block).

**Wrote** `docs/plans/0E-agent-latency-probe.md` — five-question plan + `**Files:**` block + unfrozen `## Exit checks`.

**Decisions:**
- **Placement:** `apps/playground` (camera-free harness app, the 0D sibling). No new package; `packages/agent-core` is **not** built here — it and the `Proposal`/`Action`/tool schemas / `Agent.*` states belong to milestone 2A (roadmap §5.1). The probe fixes **no** `packages/protocol` interface.
- **Runtime:** Node CLI (Node 24 built-in TS execution, 0A precedent — no `tsx`, no new dep) using global `fetch` + `ReadableStream` SSE. Fidelity caveat recorded: Node `fetch`, not the service-worker `fetch`, but identical WHATWG streaming/SSE wire format; browser CORS preflight is not a p50/p95 factor (and an extension with host permission skips it).
- **Split (mirrors 0D E3/E1):** pure harness `latency-probe.ts` (SSE parse, p50/p95, capability detection, 150-item snapshot, `LATENCY_COLUMNS` CSV, injected `fetch`-like) + owner CLI `latency-probe-cli.ts` (live run) + `node:http` stub `latency-probe-stub.ts` + Vitest `latency-probe.test.ts`.
- **Secret isolation:** key read from `process.env.LLM_PROVIDER_KEY` into `providerKey`/`Bearer` — never the identifiers `apiKey`/`API_KEY`/`ANTHROPIC_API_KEY` (boundary-lint rule 2 forbids them outside `background.ts`).

**Exit checks:** E1 "p50/p95 logged", E2 "provider and model ids entered in §8", E3 "Runs with owner's key; agent reads the numbers" — all **owner** (G7 needs a real provider under the owner's key; CI cannot produce the deciding numbers). Agent-side confidence = the keyless stub test run by `pnpm test`. No `I` rows (roadmap 0E lists no interfaces fixed here). Not frozen (lock out of scope for a docs-only session, per the 0D precedent).

**Handoff:** `CONTINUE` → execute session implements under TDD, then `NEEDS-OWNER` for the live run.

## Session 1 — execute + owner live run

`.claude/scope.json` had code paths (`apps/playground/**`), so this session implemented the `**Files:**` block under `obra-test-driven-development`.

**Implemented** (test failed first on the missing harness, then green): `src/latency-probe.ts`, `test/latency-probe-stub.ts`, `test/latency-probe.test.ts` (12 cases = E3 agent verification), `src/latency-probe-cli.ts`, `package.json` `probe:latency` script.

**Deviation from the `**Files:**` block (no ADR — not a §1–2 break, noted in plan `## Status`):** added `allowImportingTsExtensions: true` to `apps/playground/tsconfig.json`. Node 24 native TS (0A precedent, no `tsx`) requires a `.ts` import specifier for the CLI→harness import — verified empirically that `.js` and extensionless specifiers both fail to resolve under `node src/*.ts`; the flag lets `tsc` accept the `.ts` import. Tests keep `.js` specifiers (Vitest resolves both). Consistent with the plan's native-Node-TS / no-new-dependency stance.

**Verification (§5):** `pnpm --filter @gesture/playground test` → 22 passed; `typecheck` clean (after building `@gesture/protocol` + `gesture-core` dist); `node scripts/lint/boundary-lint.mjs` → OK; CLI smoke-tested. Handed off `NEEDS-OWNER` for the live run.

**Owner live run (2026-09-06):** provider **9router** via `http://localhost:20128/v1`, 10 iterations, 150-item snapshot.

| Role | Model | first-suggestion p50 / p95 | tool-calling | json_schema |
|---|---|---|---|---|
| fast | `deepseel-v4-flash` | 1574 / 2105 ms | N | Y |
| planner | `glm-5.2` | 2653 / 3886 ms | Y | Y |

**Gate G7 = MET (GO):** first-suggestion p50 ≤ 3000 ms for both models. **Caveat → 2A:** the fast model is `json_schema`-only (no tool-calling); owner's mitigation is to use `glm-5.2` for the fast role if tool-calling is required (2653 ms p50 still clears the gate). Recorded in `spike-results.md §G7`; §8 row drafted under the plan `## Status`.

**Handoff:** `DONE`. Owner logs the drafted §8 row and removes the 0E STATUS row; milestone 0E exits.

## Session 2 — finish (verify + PR)

Finish role: re-verify on a clean tree, finish the branch, open the PR. No code paths in scope; this session touches docs only.

**Verification (§5) — all green on `bd2d929`:** `pnpm --filter @gesture/playground typecheck` clean · `pnpm --filter @gesture/playground test` → 22 passed · `node scripts/lint/boundary-lint.mjs` → OK · `scripts/milestone/exit-check 0E` → `0 PASS 0 FAIL 3 OWNER 0 other | lock NONE` (all three G7 criteria are owner-gated; no FAIL/TAMPERED). Fixture replay N/A (no gesture threshold). Owner (per session-1 answers) verified E1/E3 MET.

**Finished the branch:** pushed `0E` and opened **PR #5 → master** (https://github.com/duylongpro99/gesture2browse/pull/5). Not merged (agent never merges).

**Handoff:** `DONE`. Remaining is owner-only bookkeeping: merge PR #5, log the drafted §8 row, and remove the 0E STATUS row (owner, or the `driving-a-milestone` driver via `scripts/milestone/log-decision --apply`). Milestone 0E exits. 2A consumes the §G7 numbers + the fast-model-no-tool-calling caveat.
