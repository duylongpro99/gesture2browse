# 2026-09-06 — 0E (G7 agent latency probe)

## Session 0 — plan (docs-only)

First probe session; `.claude/scope.json` has no code paths, so per the brief this session writes the plan only and hands off `CONTINUE` (the next session's scope is derived from the plan's `**Files:**` block).

**Wrote** `docs/plans/0E-agent-latency-probe.md` — five-question plan + `**Files:**` block + unfrozen `## Exit checks`.

**Decisions:**
- **Placement:** `apps/playground` (camera-free harness app, the 0D sibling). No new package; `packages/agent-core` is **not** built here — it and the `Proposal`/`Action`/tool schemas / `Agent.*` states belong to milestone 2A (roadmap §5.1). The probe fixes **no** `packages/protocol` interface.
- **Runtime:** Node CLI (Node 24 built-in TS execution, 0A precedent — no `tsx`, no new dep) using global `fetch` + `ReadableStream` SSE. Fidelity caveat recorded: Node `fetch`, not the service-worker `fetch`, but identical WHATWG streaming/SSE wire format; browser CORS preflight is not a p50/p95 factor (and an extension with host permission skips it).
- **Split (mirrors 0D E3/E1):** pure harness `latency-probe.ts` (SSE parse, p50/p95, capability detection, 150-item snapshot, `LATENCY_COLUMNS` CSV, injected `fetch`-like) + owner CLI `latency-probe-cli.ts` (live run) + `node:http` stub `latency-probe-stub.ts` + Vitest `latency-probe.test.ts`.
- **Secret isolation:** key read from `process.env.LLM_PROVIDER_KEY` into `providerKey`/`Bearer` — never the identifiers `apiKey`/`API_KEY`/`ANTHROPIC_API_KEY` (boundary-lint rule 2 forbids them outside `background.ts`).

**Exit checks:** E1 "p50/p95 logged", E2 "provider and model ids entered in §8", E3 "Runs with owner's key; agent reads the numbers" — all **owner** (G7 needs a real provider under the owner's key; CI cannot produce the deciding numbers). Agent-side confidence = the keyless stub test run by `pnpm test`. No `I` rows (roadmap 0E lists no interfaces fixed here). Not frozen (lock out of scope for a docs-only session, per the 0D precedent).

**Handoff:** `CONTINUE` → execute session implements under TDD, then `NEEDS-OWNER` for the live run.
