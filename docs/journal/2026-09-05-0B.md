# 2026-09-05 — 0B frame pump (G1)

## Session 0 — plan (docs-only)

First probe session for milestone 0B. `.claude/scope.json` has empty `tasks` (docs-only first
probe), so per `session-0-brief.md` step 2: write the plan with an exact **Files:** block, commit,
hand off CONTINUE. The execute session's scope is derived from that block.

**Inputs read:** `docs/STATUS.md`; roadmap `§3.2` (0B row) + `§9` re-plan trigger (fps < 28 →
pinned-tab fallback); `03-tech-stack §5.1` (G1 threshold: sustained ≥ 28 fps hidden, no rAF/timer);
`02-architecture §6` (`GestureFrame` v0); the 0A plan (interfaces fixed: `GestureFrame` v0,
`FixtureRecord`, `BENCH_COLUMNS`); rules `offscreen.md`, `background.md`, `protocol.md`; the existing
0A scaffold (offscreen stubs already tagged for 0B, playground playwright pattern, wxt.config,
root `hand_landmarker.task`).

**Design decisions (plan §1–5):**
- Placement: one owner = the offscreen document. `background.ts` is touched only for what the
  offscreen API forces (create the doc; surface fps, since `offscreen.md` forbids `chrome.storage`
  in the offscreen doc).
- The offscreen → SW hop crosses a trust boundary, so it gets a Zod schema: new `PumpStat` in
  `packages/protocol` (fps/frames/windowMs/delegate/hidden). Does not touch `GestureFrame` et al.
- Pump is driven by the `MediaStreamTrackProcessor` reader, never `rAF` — that is the actual G1
  property (a hidden offscreen doc never paints, so an rAF-driven pump would stall).
- Pump path is durable, not throwaway: 1B extends the same worker with the classifier; the 0A stubs
  already name 0B as their filler.
- MediaPipe model + WASM served locally as `web_accessible_resources` (no CDN); the root 7.5 MB
  `hand_landmarker.task` is copied at build, not committed twice.
- Verification: `fps-logger` unit test + `PumpStat` schema test + Playwright extension e2e
  (`--load-extension`, fake camera, hidden, ≥ 28 fps / 60 s) = the row's agent verification.

No open question needed a brainstorm. Exit checks table written (E1 owner 10-min run; V1 Playwright
gate) but **not frozen** — the lock is out of this session's scope.

**Handoff:** CONTINUE. Execute session implements the Files block.

## Session 2 — execute (Files block)

Implemented the whole **Files:** block with TDD. First commit was the owner-mandated rename of Exit-check id `V1`→`E2` (mechanical; check command unchanged) so the driver can refreeze the lock.

**Order:** `PumpStat` schema (+ test) → `fps-logger` (+ 6-case unit test) → `mediapipe.ts`/`main.ts`/`inference.worker.ts` → `background.ts` relay → `wxt.config` assets/CSP/WAR + deps → `playwright.config` + `frame-pump.e2e`.

**Result:** E2 gate **passes** — 60 s, WebGL (GPU) delegate initialised in headless Chromium, **mean 30.2 / p05 30.0 / min 30.0 fps** (pinned at the 30 fps fake-camera cap), 30/30 windows, zero `requestAnimationFrame` in the offscreen+worker bundles. `tsc`, eslint, boundary-lint, extension vitest (6), protocol vitest (24) all green. Numbers in `docs/spike-results.md §G1`.

**Decisions / deviations:**
- `PumpStat.delegate` reuses the existing `DelegateSchema` (`webgl`/`wasm`) rather than the plan §3 `GPU`/`CPU` — protocol rule forbids synonyms; MediaPipe's `GPU`/`CPU` mapped in `mediapipe.ts`.
- WXT's esbuild left `new Worker(new URL('./x.ts', import.meta.url))` untransformed (no worker chunk, `.ts` ref survived) → switched to Vite's `?worker` import, which bundles the worker with MediaPipe (144 kB chunk).
- Extension code uses the WXT `browser` global (no `@types/chrome`); `browser.runtime.getURL` is typed to `PublicPath`, so the build-injected `/wasm` and `/models` URLs are derived from `getURL('/')`.
- Added `wasm-unsafe-eval` to the extension-pages CSP (MediaPipe instantiates WASM from fetched bytes).

**Finding → NEEDS-OWNER:** Chrome reports `document.hidden === false` for an offscreen document (it is not a backgrounded tab). The plan's "`hidden` must be true in the gate" cannot hold for the offscreen container. E2 records `hidden` verbatim (`[false]`) and instead asserts the gate's two testable halves — no rAF, and sustained rate with no foreground surface (only `about:blank` open). Owner is asked to confirm this reading of E2 and of their own E1 run.

**Handoff:** NEEDS-OWNER — (1) confirm the `hidden` interpretation; (2) run the 10-minute M1 hidden-doc check (E1). Steps in `docs/sdd/0B/handoff.md`.

### Owner answers (2026-09-05)

- **Q1 → A:** accept the reading — the offscreen document has no visible surface; keep E2 exactly as is, no extra test. (No code change; `## Status` finding marked resolved.)
- **Q2 (E1):** owner is running the 10-minute M1 check now and will paste the numbers into `spike-results.md §G1` themselves — do not wait. Driver refroze the exit-check lock at `14f62d9` (E1+E2); commit `506d725` accepted as-is.

Did everything not dependent on E1: recorded the proposed §8 **G1 = GO** decision (30.0 fps p05, WebGL, no rAF) + the `document.hidden` platform note in the plan `## Status`; confirmed E2 green via `scripts/milestone/exit-check 0B --fast`. Rewrote `handoff.md` to `NEEDS-OWNER` with only E1 pending.

### E1 confirmed → DONE (2026-09-05)

Owner ran the 10-minute hidden-doc check on the M1: **30.5 fps sustained ≥ 28** over 10 min. Completed the G1 **E1** block in `spike-results.md` from the owner's report (Setup = M1 unpacked build, Chrome backgrounded 10 min, numbers from `chrome.storage.session`; Result = 30.5 fps; Gate met = Y) and filled the go/no-go table G1 row (Y). Gate **G1 = Y** on both datapoints.

Two things noted, not fixed here:
- **Camera-grant workaround** the owner needed (offscreen docs can't prompt; `grant-camera.html` is still the 0A stub): open `grant-camera.html`, `getUserMedia({video:true})` in DevTools, Allow-not-this-time, reload. Recorded under §G1 E1 as a **0C / G2 input**.
- The owner's `30.5` had landed in the **G2** Result cell (misplaced — an fps unrelated to the camera-flow gate). Left untouched per "do not change the owner's number"; flagged in the plan `## Status` for the owner to clear. The G1 E1 block was completed from the owner's message, not by moving that number.

Marked §8 **G1 = GO** final in the plan `## Status`; rewrote STATUS row and this journal.

**Handoff:** DONE. Owner opens the PR to `master` and removes the STATUS row on merge; agent does not merge or edit §8.

## Session 3 — finish (verify + branch + PR)

Finish role per `session-3-brief.md`: re-verify, finish the branch, open the PR. No code paths in scope — this role cannot fix a failing check.

**Verification (clean tree, HEAD `343e716`):**
- `pnpm typecheck` → 7/7 successful
- `pnpm lint` → 5/5 successful + `boundary-lint: OK (no trust-boundary violations)`
- `pnpm test` → all green (gesture-core 13; extension/protocol vitest green; playground/page-index no test files)
- `scripts/milestone/exit-check 0B` (full) → **1 PASS 0 FAIL 1 OWNER 0 other | lock OK (frozen at `14f62d9`)** — E2 mechanical PASS, E1 OWNER met (owner's 30.5 fps / 10 min logged in `spike-results.md §G1`)
- No thresholds changed (docs-only finish) → no fixture replay.

**Branch:** `0B` had no upstream and was not on `origin`; pushed `0B` → `origin/0B` (new branch). master +2 / 0B +11 at push time — no rebase (forbidden by brief; GitHub does the 3-way).

**PR:** opened **#2** → `master` (https://github.com/duylongpro99/gesture2browse/pull/2), title "0B: G1 frame pump probe …". Did **not** merge; did **not** edit roadmap §8. Owner logs §8 (G1 = GO + `document.hidden` note), merges, removes the STATUS row.

**Left for owner:** `docs/spike-results.md §G2` stray `30.5` — out of this session's scope; owner to clear.

**Handoff:** DONE.
