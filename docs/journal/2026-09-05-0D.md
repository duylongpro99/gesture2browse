# 2026-09-05 — 0D click-dispatch survey (G5)

## Session 0 — plan (docs-only)

First probe session for milestone 0D. `.claude/scope.json` has empty `tasks` (docs-only first
probe), so per `session-0-brief.md` step 2: write the plan with an exact **Files:** block, commit,
hand off CONTINUE. The execute session's scope is derived from that block.

**Inputs read:** `docs/STATUS.md`; roadmap `§3.2` (0D row: 20-site survey, synthetic vs CDP, runs in
Playwright; Exit = owner spot-check + dispatch default in §8) + `§3.3` (G5 owner spot-check) + `§8`
(dispatch-default decision pending, unblocks 1A/1C); `docs/plans/README.md` (five-question form,
Exit-checks/Status form); the 0A plan (interfaces fixed by 0A; bench CSV convention); the 0B plan as
the golden probe-plan pattern; rules `fixtures-and-tests.md`, `content.md` (synthetic = fallback
input), `background.md` (CDP = primary input); `spike-results.md §G5` (the empty G5 template to fill).

**Design decisions (plan §1–5):**
- Placement: one owner = the `apps/playground` harness (`fixtures-and-tests.md` covers
  `apps/playground/**`, `fixtures/**`). The survey is the sibling of the 0A bench harness — a
  camera-free Playwright experiment emitting an outcome table. It does **not** touch `apps/extension`,
  `packages/protocol`, or `gesture-core`, and does not build/run the extension.
- Two techniques model the extension's two production input paths from outside: **synthetic** =
  content-script fallback `dispatchEvent` (untrusted, `isTrusted===false`); **CDP** = the SW's
  primary `chrome.debugger` path, via a Playwright `CDPSession` `Input.dispatchMouseEvent` (same
  DevTools domain → trusted-input fidelity transfers).
- No `protocol` schema: nothing crosses a component boundary at runtime (contrast 0B's `PumpStat`).
  The outcome record/CSV is playground-internal; 0A's bench CSV is a *convention to copy*, not a shape
  to reuse. The 0D row has no "Interfaces fixed here" → no `I` exit rows.
- No new dependency (Playwright + bundled CDP already in tech-stack; playground already has
  `@playwright/test`) → no `pnpm-lock.yaml` change expected.
- ~15 local fixtures under `fixtures/dispatch/**`, one discriminating mechanism each (delegation,
  pointerdown, isTrusted guard, native select, window.open/target=_blank user-activation,
  same/cross-origin iframe, canvas hit-test, closed shadow DOM, label→checkbox, contenteditable);
  the live list is owner work (real framework/app instances), owner spot-checks 5 reported failures.
- Verification: Vitest unit for the pure summariser (`summarizeOutcomes`/`recommendDefault`/
  `outcomesToCsv`) + Playwright survey over the local fixtures (the row's agent verification, "Runs in
  Playwright"); the survey asserts run soundness (a well-formed row per fixture × technique), not
  "synthetic must pass" — synthetic *failing* on the hard cases is the finding.

No open question needed a brainstorm (all five answered from the roadmap row, rule files, and the
0A/0B golden path), so no NEEDS-OWNER. Exit checks table written (E1/E2 owner from the Exit cell;
E3 Playwright from the agent-verification cell) but **not frozen** — the lock is out of this session's
scope; the execute/driver session freezes it once the commands run.

**Handoff:** CONTINUE. The execute session implements the **Files:** block with TDD, records the
per-site table + recommended default in `spike-results.md §G5`, and freezes the lock; the owner then
runs the live list, spot-checks 5 failures (E1), and logs the dispatch default in §8 (E2).
