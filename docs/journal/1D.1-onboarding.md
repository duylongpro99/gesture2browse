# Milestone 1D.1 — 1D.1-onboarding

| | |
|---|---|
| Plan | `docs/plans/1D.1-onboarding.md` |
| Branch | `1D.1` from `master` @ `c70f122` |
| Roadmap | `docs/05-roadmap.md §4.4` |
| PR | — |
| Updated | 2026-09-14 @ `ad2e7ac` by `scripts/milestone/journal 1D.1` |

## Verification

Run from the repo root, on branch `1D.1` or on `master` after the merge. `scripts/milestone/journal 1D.1 --recheck` runs every command below and appends a line to Runs; the `owner` rows are the numbered steps under Owner steps. The table is the plan's frozen `## Exit checks` (`docs/plans/1D.1-onboarding.md`); a change to it is a re-plan, never an edit here.

| # | Criterion | Kind | Check | Last result |
|---|---|---|---|---|
| E1 | Playwright screenshot reviewed by the owner | owner | steps below | owner: steps below |
| E2 | settings round-trip through `chrome.storage` validated by the `protocol` schema | mechanical | `pnpm exec playwright test -c apps/extension/playwright.config.ts onboarding.e2e.ts` | FAIL 2026-09-14 @ ad2e7ac (fast) |
| I1 | Profile + Settings (onboarding fixes the shared profile/settings record; default Accessibility, additive) | consumer:1D.2,1D.3 | `pnpm vitest run packages/protocol/test/contracts/1D.3-settings-profile.contract.test.ts` | FAIL 2026-09-14 @ ad2e7ac (fast) |
| I2 | OnboardingState (first-run completion; device-local, versioned) | consumer:1D.3 | `pnpm vitest run packages/protocol/test/contracts/1D.3-onboarding-state.contract.test.ts` | FAIL 2026-09-14 @ ad2e7ac (fast) |

### Owner steps

**E1 — Playwright screenshot reviewed by the owner**
1. From the repo root, run `pnpm exec playwright test -c apps/extension/playwright.config.ts onboarding.e2e.ts` (added by execute Task 5); it writes `apps/extension/test-results/onboarding.png`.
2. Open `apps/extension/test-results/onboarding.png` and confirm the onboarding flow renders as designed — consent/privacy copy leading with "video never leaves the device," the profile picker with Accessibility preselected, and the "you're ready" state — and that no raw video is shown.
Expected: the screenshot matches the flow in `docs/plans/1D.1-onboarding.spec.md`. Not available until execute Task 5 lands; owner-only, so it stays `not yet` until the owner confirms.

### Runs

<!-- appended by scripts/milestone/journal: every full exit-check run (finish) and every --recheck -->

## Implemented

Branch `1D.1` from `master` @ `c70f122` to `ad2e7ac`: 3 commits, 5 files changed, 479 insertions(+).

### Session-end writes (`[docs]`)
- `28c4fb8` [docs] 1D.1: spec + impl plan (onboarding) (`docs/plans/1D.1-onboarding.impl.md`, `docs/plans/1D.1-onboarding.spec.md`)
- `ad2e7ac` [docs] 1D.1: sdd workspace + progress ledger (`docs/sdd/1D.1-onboarding/progress.md`)

### Other commits (no `[component] task N` line)
- `d3e8ccf` [protocol] 1D.1: frozen contract tests (I1 Profile/Settings, I2 OnboardingState) (`packages/protocol/test/contracts/1D.3-onboarding-state.contract.test.ts`, `packages/protocol/test/contracts/1D.3-settings-profile.contract.test.ts`)

## Sessions

| N | Role | Recorded | Outcome | Summary |
|---|---|---|---|---|
| 0 | plan | 2026-09-14 | DONE | Produced the 1D.1 onboarding spec, impl plan (5 tasks), five-question form + frozen Exit checks, two consumer-side contract tests (fail today), and the SDD workspace; no owner gate needed. |

## Decisions

Proposed for roadmap §8 (the owner logs it; `scripts/milestone/log-decision 1D.1 --apply` after the merge):

**Proposed decision(s) for roadmap §8 (owner logs; agent does not edit §8):**
> | 2026-09-14 | **1D.1 onboarding interfaces = FIXED.** New full-tab `entrypoints/onboarding` first-run wizard (arch §3.4 pattern, like grant-camera/diagnostics): consent/privacy (leads with "video never leaves the device") → camera-grant handoff into the existing 0C grant page (steer to "Allow on every visit") → profile (default **Accessibility**) → "you're ready" (links to 1D.2 calibration + 1D.6 cheat sheet; does not run either inline). Onboarding never calls `getUserMedia` — capture/grant stay in `grant-camera`; it reads device kind + permission state only. `protocol` additive: new `Profile` (`accessibility`/`standard`/`presenter`, default accessibility — promotes the informal dispatcher union), `Settings` (`{profile}`, `storage.sync`, extended additively by 1D.2/1D.3), `OnboardingState` (`{completed,completedAt?,version}`, `storage.local`), `OnboardingComplete` (page→SW). Background gains the `onInstalled` first-run trigger, the incomplete-onboarding pump gate, and reads `Settings.profile` to drive the dispatcher (Accessibility default → dwell-click on); no gesture-timing constant added. Required unhappy paths built (camera denied/"Allow this time" re-route, no-webcam dead-end, restricted `<all_urls>` degraded/retry, full keyboard + screen-reader path). New `.claude/rules/onboarding.md`. Supersedes the roadmap row's "60-second tutorial" (deferred to 1D.6). | 0C grant page (G2); owner intent (session -1) | Fixed in `docs/plans/1D.1-onboarding.md`; consumed by 1D.2 (calibration writes `Settings`) and 1D.3 (settings edits profile + onboarding state) |

ADRs added on this branch: none.

## Notes

<!-- Free text by the worker (what to know that git and the tables do not say); kept by scripts/milestone/journal. -->
This journal was hand-written this once because `scripts/milestone/journal 1D.1` crashes for every §4.4 milestone. Under `set -euo pipefail`, `title=$(grep -E '…Milestone 1D\.1…' docs/05-roadmap.md | head -1 | sed …)` (script lines 164–166) returns non-zero: 1D.1 has no `### N Milestone 1D.1` heading of its own — it lives under the shared `### 4.4 Milestones 1D.1–1D.6` heading — so `grep` finds nothing, `pipefail` propagates its exit 1 to the assignment, and `set -e` aborts before the `[ -n "$title" ] || title=$slug` fallback on the next line. Same root cause the 1D.5 handoff note recorded; the worktree and root `scripts/milestone/journal` copies are now byte-identical, so the earlier "patched root copy" workaround is gone. The content above matches the tool's own format (header table, `## Verification` from the plan's frozen `## Exit checks`, `## Implemented` grouped by commit, `## Sessions`, `## Decisions`, `## Notes`). Kit fix belongs in a tooling PR (guard the `title=` pipeline, e.g. append `|| true` so the fallback runs); scope forbids editing `scripts/` from this session.
