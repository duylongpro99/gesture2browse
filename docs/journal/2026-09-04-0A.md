# 2026-09-04 — Milestone 0A — session 1 (plan)

Worktree `human-gesture-0A`, branch `0A`. Role: plan (produce spec, impl plan, plan, exit checks, contract tests, SDD workspace).

## What happened

- **Task 0.1** — applied 04-feasibility §4 to docs 01–03 (commit `93ddfb3`): worker frame pump (`MediaStreamTrackProcessor` → transferred `ReadableStream` → Worker), WebGL→WASM delegate (no WebGPU), `HandLandmarker` + own classifier with a mandatory "none" class (retires Model Maker), hybrid click dispatch (content-script default, optional `debugger`), agent loop in the service worker (BYOK; companion → Phase 3), critic + propose-only default, dwell-click MVP mode, 300 ms stable-tracking gate, updated hold times / pinch metric, full-tab camera grant page, presenter persona (Priya) deferred to Phase 3 (v1 range ≤ 1.5 m), PRD Q2–Q4 answered. The agent-runtime/models/provider rows were already provider-agnostic (marked *Done* in §4) and left unchanged. Owner approved the diff, including a few consistency edits §4 did not enumerate (PRD §12 WebGPU risk row, PRD §8 key-storage line, architecture §5 FSM rules, tech-stack dispatch/browser rows) and leaving the superseded PRD §11 / architecture §11 as-is.
- **Spec questions Q1–Q4** batched via handoff; owner said "proceed with planning". Adopted the recommendations: raw MediaPipe landmarks in fixtures; all fixed shapes in `packages/protocol`; provisional `Intent` v0 (1A finalizes); synthetic placeholder fixture + y4m for camera-free CI.
- **Planning artifacts** — `0A-scaffold.spec.md` (five questions + interface shapes), `0A-scaffold.impl.md` (8 TDD tasks with exact Files blocks; Global Constraints name the owning rule files), `0A-scaffold.md` (five-question record + frozen Exit checks E1–E3, I1–I3 + Status), three frozen contract tests under `packages/protocol/test/contracts/`, and the SDD workspace `docs/sdd/0A-scaffold/` with `progress.md`.

## Decisions / notes

- **Boundary refinement surfaced:** `gesture-core` must import `Intent`/`GestureFrame`/`FixtureRecord` from `@gesture/protocol`, but `.claude/rules/gesture-core.md`'s "May depend on" list omits protocol. The rule gains `@gesture/protocol` (types only) in impl Task 3 (this milestone; CLAUDE.md §5). Not a §3 deviation — the correct boundary being completed. Flagged for owner in §8 proposed decisions. This planning session could not edit `.claude/rules/` (out of write scope).
- `Intent` v0 is provisional; 1A owns the final. Stated in the plan so 1A does not treat it as frozen.
- Task 0.3 (`CLAUDE.md`) already satisfied by the repo — noted, no impl task.

## Handoff

`outcome: DONE`. Commits this session: `93ddfb3` (0.1 doc edits), `dbba2a1` (spec), `b8ae7cc` (impl plan), `f3a8523` (plan + exit checks), `f0cc907` (contract tests), `fd5dc9b` (SDD ledger), plus the session-end `[docs]` commit. Next session runs execute Task 1.
