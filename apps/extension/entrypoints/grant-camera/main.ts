// 0A stub. The camera-grant flow (1D.1 / G2): a full-tab page that calls
// getUserMedia so the offscreen document inherits the permission, plus a
// permissions.query pre-check and "Allow this time" detection. Empty in 0A.
export {};
